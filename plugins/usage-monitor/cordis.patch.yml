# dsh-usage-stats bundle patch: inserts its plugin row into the web profile
# roster. The bundle is the official dsh plugin shape.
- insert:
    - id: usage-stats
      name: '@abcdefu_cja/dsh-usage-stats'

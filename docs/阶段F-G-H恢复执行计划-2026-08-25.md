# 阶段 F/G/H 恢复执行计划（2026-08-25）

> 基线：上一会话中断前的实际落盘状态 + `.project-log` C0050/C0051。用户已确认恢复并完成阶段 F/G/H 全部后续计划；专家设计和用户画像不纳入当前收口批次，但列入后续计划。
> 原则：先核验官方能力和本地 profile，再实现、真实验证、部署；缺官方接口时寻找可审计等价实现，但不使用提示词伪装能力。

## 一、当前事实基线

| 事项 | 状态 | 证据/限制 |
|---|---|---|
| 官方版本线 | 已核验到 `0.1.1-rc.2` 的 `next` 线 | 需每次同时核对 npm 版本、peer 依赖、桌面内嵌 API 和实际平台包 |
| F2 Codex Bundle | 已安装，运行时可见 | provider 列表含 `codex`；2 个 Codex 实例真实并发探针通过 |
| F2 Claude Code Bundle | 已安装并能启动到上游 | 最新真实桌面只读探针已走 `claude-code` 轨道 B 并完成；此前的 `402` 记录保留为历史阻塞证据，不代表本次结果 |
| F2 双轨代码 | ✅ 已完成真实收口 | Host 已记录 execution track、provider、实际 provider、失败/回退原因；主协调固定轨道 A |
| F6 worktree | ✅ 已完成真实闭环 | 真实桌面已验证短路径 worktree、轨道 B 写入、原项目隔离、apply/discard、取消后清理、越界/非工作台分支/错误状态拒绝，以及原项目冲突时 apply 返回 409 且不覆盖原文件 |
| G4 校准闭环 | 代码已落盘 | 支持合适/过度/不足/轨道不合适人工评分、14 天回放、策略版本和人工采用；门槛为 20 条任务 + 10 次人工验收 + 10 条评分 |
| H2 平台载荷 | ✅ 已完成备份副本兼容核验 | SQLite、Job Panel、profile、版本/peer/API/Windows x64 平台包证据已核对；生产 profile 不迁移 |
| H1/H3 | ✅ 已完成本轮回归与部署收口 | 进程内 smoke、真实 GUI、部署哈希、普通重启和新启动日志均已核对；F6 真实 worktree 仍受 Git 会话前置条件限制 |

## 二、重新整理后的待办清单

| 顺序 | 编号 | 工作项 | 状态 | 完成条件 |
|---|---|---|---|---|
| 0 | RECOVERY-BASELINE | 恢复中断会话基线 | ✅ 已完成 | 工作区备份、provider 清单、平台包、源码/安装副本哈希、进程状态和 G4 日志起点已记录；未迁移生产 profile |
| 1 | H2-A | 官方版本/peer/API/平台包四方核验 | ✅ 已完成 | 最新构建已部署；实体 manifest、MCP 和三类插件资源哈希与工作区一致；Windows x64 provider 包证据已保存 |
| 2 | G4-A | 真实路由样本和人工评分 | 观测持续 | 滚动 14 天，至少 20 条路由任务、10 次人工验收、10 条人工评分 |
| 3 | G4-B | 回放对比与受控采用 | 等待 G4 门槛 | 生成新策略版本；展示旧/新策略差异；用户一键采用后才改变默认策略，否则保持 `benchmark-v1` |
| 4 | H1-A | 不依赖外部 provider 的全量回归 | ✅ 已完成 | 14 项进程内 smoke、F1/F3/F4/F5/G1 GUI、失败/回退/并发/停止恢复通过；客户端加载权限已恢复 |
| 5 | H2-B | 本地升级兼容收口 | 备份副本核验完成，生产迁移不做 | 在可读备份副本上验证 SQLite、Job Panel、profile 恢复和版本/peer/API/平台包；生产 profile 不做未经验证的迁移 |
| 6 | H3-A | 部署前校验 | ✅ 已完成 | 源码/安装目录 SHA-256 一致 |
| 7 | H3-B | 普通重启与新启动日志 | ✅ 已完成 | 日志确认知识工具、Job 登记闸门和 80 路由；普通模式启动且调试端口关闭 |
| 8 | H3-C | F/G/H 大阶段蒸馏与日志收口 | ✅ 已完成（保留 F6/G4 边界） | 已蒸馏 C0064-C0068 为 5 条 `01-Inbox` 草稿；补齐 C0068-C0069 日志；未把 G4 门槛或 F6 真实桌面阻塞改写为通过 |
| 9 | F2-A | Codex 轨道 B 真实收口 | ✅ 已完成 | 真实工作台只读 worker 走 B；任务记录保存 provider/track/结果；Codex 返回 `CODEX_TRACK_B_OK` |
| 10 | F2-B | Claude Code 轨道 B 真实收口 | ✅ 已完成 | 真实工作台只读 worker 走 B；任务记录保存 provider/track/结果；Claude 返回真实只读交接报告 |
| 11 | F6-A | Codex + worktree 真实桌面验证 | ✅ 已完成 | 在线 Git 项目中短路径独立 worktree 检出成功；Codex 轨道 B 写入并自动提交；原项目 apply 前不变、apply 后可见；discard 与测试清理完成 |
| 12 | F6-B | worktree 失败、停止和清理验证 | ✅ 已完成 | 外部进程失败或取消后隔离区保留且可删除；越界路径、非工作台分支和错误状态被拒绝；原项目存在冲突时 apply 返回 409 且原文件保持不变 |
| 13 | EXPERT-DESIGN | 工作台专家设计 | 后续阶段，待当前 F/G/H 收口后启动 | 明确专家角色、职责边界、触发条件、输出格式和人工确认边界；完成方案评审后再开发 |
| 14 | P6.2 | 用户画像 | 后续阶段，待当前 F/G/H 收口后启动 | 明确画像数据来源、隐私边界、更新策略、可见性和撤销机制；完成方案评审后再开发 |

## 三、旁路但不阻塞 F/G/H

- `01-Inbox` 8 条草稿人工审核。
- C4/R5 3-7 天真实使用观察和验收第 7 条证据。
- D1 `signal timed out` 连续 20 个历史会话确认。
- D3 朋友机器 profile 重置验证。
- D4 独立仓库依赖对齐、D5 风格页收尾。

这些事项继续记录，但不与 F2/F6/H2 的真实能力收口混在同一验收结论中。

## 四、执行批次

### 批次 A：恢复与静态核验

RECOVERY-BASELINE → H2-A。先使用已有备份和停机 profile 副本；不覆盖生产 profile，除非备份、哈希和恢复路径已确认。

### 批次 B：路由、回归与部署前收口（已完成主链路）

G4-A（持续观测）→ G4-B（等待门槛）→ H1-A → H2-B → H3-A/H3-B → H3-C。H3-C 已完成归档；本批次主链路完成，当前仅保留 G4 观察门槛。

### 批次 C：外部 provider 最后验证

F2-A → F2-B → F6-A → F6-B。历史上的 Claude `402` 仍单独记录为外部账户阻塞，但最新 F2-B 真实只读验收已通过，不得把历史记录覆盖当前结果。

### 批次 D：归档

补充 provider/账户阻塞说明，部署后必须读本次启动日志；H3-C 已完成归档，F6 已完成，G4 观察门槛仍未解除，因此仍不宣布 F/G/H 全部收口。

## 五、当前阶段明确不做的事

- 不用提示词宣称拥有独立 `cwd`、完整工具隔离或 provider 能力。
- 不把 Claude 的 `402` 改写成“兼容失败”或“测试通过”。
- 不把锁文件条目当成 Windows 原生包已安装。
- 不在 G4 样本不足时自动改阈值。
- 当前 F/G/H 收口阶段不启动专家设计或用户画像实现；两项已登记到后续计划，不能视为取消。

## 六、后续阶段计划（当前 F/G/H 收口后）

### 1. EXPERT-DESIGN：工作台专家设计

- 启动条件：F/G/H 主线完成，或外部 provider 已明确阻塞并完成阶段归档。
- 方案范围：专家角色与职责、适用任务类型、触发/退出条件、工具与权限边界、输出结构、人工确认和审计记录。
- 完成门槛：先完成方案评审和边界确认，再拆分开发任务；不以提示词文本替代真实能力边界。

### 2. P6.2：用户画像

- 启动条件：专家设计边界明确后，单独立项，不与专家预设混合。
- 方案范围：画像数据来源、用户可见性、隐私与脱敏、更新/纠错/撤销、跨会话使用范围和存储隔离。
- 完成门槛：先完成数据模型、权限和生命周期方案评审，再进入实现与迁移；未经确认不读取或改写生产画像数据。

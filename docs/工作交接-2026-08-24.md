# 工作交接（2026-08-24，历史快照）

> **状态提示（2026-08-25）：本文部分明细仍停留在 C0039/C0036 快照；最新未执行计划和执行顺序见 `docs/未执行计划重排-2026-08-25.md`，最新状态见 `.project-log/status.md` 的 C0051。**

> **C0052 更新：上一会话中断后的最新事实和阶段 F/G/H 执行顺序见 `docs/阶段F-G-H恢复执行计划-2026-08-25.md`；本文不再作为当前执行清单。**

> 用途：给下一个会话/开发者快速接手当前工作台改造项目。所有未完成计划已同步到 `.project-log/status.md`（阶段总览）。
> 交接人：Codex（root）；交接日期：2026-08-24；最近提交：`b627876`（功能）+ `7a28e49`（文档），分支 `main`。

---

## 一、项目背景

DeepSeek Harness Desktop 工作台（`dsh-workbench` 插件）的持续改造：知识库（Adaptive KB-RAG）评测收口 + 多代理并行与对话化改造。完整历史见 `.project-log/changelog.md`（编号 C0001-C0039）。

## 二、当前状态快照

> 2026-08-24 晚间续作：F1/F4/F5、G1/G2/G3 已完成真实或专项验收；G4 的 14 天路由观测已启动；H1 当前可用轨道全量回归通过。F2/F6/H2 因当前 0.1.1-rc.2 缺少兼容 provider / 单次 cwd 能力而暂停，详见 `docs/工作台改造计划-2026-08-23.md` 最新表格。

- 工作目录：`C:\YourWorkbench`；分支 `main`；最近提交 `9fb4c39`；C0037-C0039 为当前工作区改动，尚未提交。
- 最近记录编号：C0039。
- 插件源码：`plugins/workbench/`（`lib/client.js` 客户端、`lib/host/index.js` host）。
- 部署位置：`C:\Users\wang2\AppData\Local\Programs\DeepSeek Harness Desktop\resources\app.asar.unpacked\node_modules\dsh-workbench\`（client.js / lib/host/index.js）。
- 部署前备份：`backups/dsh-workbench-client-before-e-20260824-011718.js`、`dsh-workbench-host-before-e-20260824-011718.js`（E 阶段前版本）；`backups/dsh-workbench-host-before-f1-20260824.js`（F1 前版本）；`backups/dsh-workbench-host-before-f3-20260824.js`（F3 前/F1 完成版本）；`backups/dsh-workbench-host-before-f3-boundary-correction-20260824.js`（F3 首次部署版本）。
- 全局 dsh CLI：0.1.1-rc.2（与内嵌核心一致）。
- 当前应用已再次普通重启并加载 F/G 当前最终版本；安装目录 host/client 与源码 SHA-256 一致，`9225` 调试端口已关闭。运行日志确认 `knowledge_search + knowledge_read tools registered` 和 78 条工作台路由注册成功。

## 三、已完成 ✅

### 知识库 R3-R5（阶段 A/B/C）
- A1-A4：R3 逐句 groundedness 评分与修正落地、桌面复验、20 题门禁 1.0、auditLevel 切 ref+groundedness（C0017/C0019/C0020）。
- B1-B5：cross-encoder 重排三组对比（默认不开启）、评测指标补全（completeness/faithfulness/缓存命中/真实调用）、离线+真实运行双门禁（C0021/C0022）。
- C1-C3：全量回归、36 文件哈希 0 差异、验收 8 条 7/8（C0023）；未命中进候选池修复（C0024）。
- 对应冒烟：`smoke-knowledge.mjs`、`smoke-knowledge-auto.mjs`、`run-knowledge-eval.mjs --gate`、`gui-r3-groundedness-eval.mjs --gate-only`。

### 任务中心/编排修复
- C0025 手动状态不被同步覆盖（locked）；C0026 无来源会话启动兜底；C0027 主代理失败透传真实错误。

### 多代理 E 阶段（C0031-C0035，已部署）
- E1 快速问答直发原生会话（`session.prompt`），零编排记录。
- E2 对话流并行卡片：一行摘要 + 可展开子代理列表 + 终态自动折叠；子代理运行情况在右侧工具栏"协作"页签（C0033）。
- E3 写操作确认门：host 默认 readOnly + 方案生成 readOnly 契约（C0032）+ 写操作确认卡 + 审批浮层（最佳努力）。
- E4 详情抽屉：方案/代理状态/日志/记忆/决策五页签；AI 协作整页保留。
- E5 成本护栏：并行上限默认 3、预计调用/token 显示、停止/继续执行常驻。
- 补充：模型三档（全自动/主手动/全手动）、失败自动换模型重跑（最多 2 次）、单子代理重试、超时 15min→10min、删除右上角状态胶囊（C0034/C0035）。

### 多代理 F1（C0037，已部署）
- worker 在官方能力可用时走 continuable/reportDelivery 异步回传，旧运行时回退 one-shot。
- 动态并行槽替代批次等待；快 worker 完成后立即推进已满足依赖的任务。
- 换模型与同模型重试共享总预算，避免最坏执行次数超出成本护栏。

### 多代理 F3（C0038，已部署）
- 工作台编排/恢复/单代理重试/继续优化注册为主会话归属的官方 `orchestration-*` Job，工作台任务库保存 jobId。
- 官方 jobs/model `job_kill` 与工作台停止共用 AbortController；completed/failed/killed 及最终输出从工作台终态同步。当前 rc.2 的人类 Job Panel 是只读列表，没有停止按钮，人类停止仍走工作台入口。
- 新增 `smoke-orchestration-jobs.mjs` 与 `gui-f3-job-panel-e2e.mjs`；12 组全量 smoke 全过；源码与安装目录 SHA-256 同为 `A1398978B51FFF8F9D7BA68687FB23A77FF2DC192D23063EB243F970343FDE0D`；真实桌面实测面板显示 running，工作台停止后更新 cancelled。

### 工程/规划
- 阶段 E-H 完整方案与行业调研（C0028-C0030，docs/多Agent协作改造方案-2026-08-24.md）；dsh CLI 升级 0.1.1-rc.2。

## 四、进行中 ⏳

1. **E 阶段 + F1 应用内实测**（下一步首选）：F3 已通过；继续验证简单问题直发原生零编排记录；复杂任务并行卡片/写操作确认/详情抽屉/停止继续；模型:全自动/主手动/全手动；失败自动换模型；重试此子代理；快 worker 结束后父任务被提前唤醒、依赖项不等待无关慢 worker。
2. **C4 观察期**（3-7 天真实使用）：候选池增长/知识增长/审计样本/无回归 → C5 标记 S5 完成（验收第 7 条依赖此数据）。
3. **01-Inbox 4 条蒸馏草稿人工审核**：groundedness 轻量实现路径、cross-encoder 性价比、LLM 评估器稳定性三坑，审核后移入 02-Atomic。

## 五、阶段 F 后续 ⬜

### 阶段 F：并行引擎升级（接官方 RC.8 能力，内嵌核心 0.1.1-rc.2 已含）
- F1 轨道 A 异步收集 + reportDelivery 完成即唤醒主代理（不等整批）——**代码与自动化验收已完成，待桌面重启实测**。
- F2 轨道 B：Codex / Claude Code Profile Bundle 按需接入（等 E 实测通过后试点，先 Codex 后 Claude Code；装前备份 profile 三件套；只读起步）。
- F3 官方 Job Panel 统一可见——**已完成，真实桌面 E2E 通过**；工作台编排注册为会话归属的 `orchestration-*`，工作台停止后面板同步为 cancelled；rc.2 人类面板为只读列表。
- F4 web_search 并发（知识检索 webFallback 多问题并发）。
- F5 多模态链路验证（/goal、/plan 图文输入）。
- F6 git worktree 隔离（写型子代理可选，默认关）。

### 阶段 G：自动并行
- G1 只读/检索自动并行（默认开，可一键关）；G2 写操作建议卡；G3 智能分流（简单→单代理、中等→2-4、复杂→更多/轨道 B）；G4 数据驱动调优（1-2 周真实数据）。

### 阶段 H：回归与收尾
- H1 并行正确性专项（并发上限/乱序/失败重试/唤醒及时性/停止恢复/双轨混跑）；H2 官方升级兼容（SQLite 迁移备份、Job Panel、Bundle 实测）；H3 部署 + SHA-256 + 日志补录 + 大阶段蒸馏（R3-R5 与 E 阶段按大阶段策略蒸馏）。

### 阶段 D：其他遗留
- D1 signal timed out（C0003）真实桌面最终确认（连续打开 20 个历史会话）。
- D2 01-Inbox 审核（同进行中第 3 条）。
- D3 朋友机器 profile 重置后验证。
- D4 独立仓库 `dsh-workbench` 依赖对齐 rc.2（低优先）。
- D5 风格页半成品收尾。

## 六、环境与操作速查

- 校验：`node --check plugins/workbench/lib/{client,host/index}.js`；eslint 需读 pnpm 共享存储（沙箱外运行）。
- 冒烟：`node plugins/workbench/tools/smoke-{client-load,orchestration,worker-failover,collab,workflow,style,knowledge,memory,watchdog}.mjs`。
- 部署：源码 → 安装目录 `resources/app.asar.unpacked/node_modules/dsh-workbench/lib/`，部署前备份、部署后 SHA-256 校验。
- 日志：每次改动按 AGENTS.md 用 project-log-update 技能补录，编号连续（下一个 C0036）。
- 蒸馏：大阶段完成时用 project-log-distill 技能，先进 01-Inbox 待审核。

## 七、风险与注意事项

- 应用不重启不加载新代码；升级/动 profile 前备份 `harness-home`（SQLite 格式不兼容）。
- 子代理审批浮层是"最佳努力"监听，官方审批 UI 兜底；写门是计划级（readOnly 契约），非逐次工具调用拦截。
- 重型 GUI E2E（p26/p27/trace-e2e）需在真实应用内做，建议用户实测时顺带验证。
- 编排执行需要在线主会话；"总结工作台知识库的评测体系"编排上次因 gpt-5.5 上游 502 失败，方案与子代理结果完好，可重试产出逐句审计样本。

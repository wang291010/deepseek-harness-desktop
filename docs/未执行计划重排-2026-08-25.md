# 未执行计划重排（2026-08-25，历史基线）

> **2026-08-25 更新：用户已确认恢复 F/G/H 全部后续计划；阶段执行顺序改用 `docs/阶段F-G-H恢复执行计划-2026-08-25.md`。本文件保留此前等待审核时的总待办，不再作为当前执行清单。**

> 基线：`.project-log/status.md` 的 C0050。本文是当前唯一的执行顺序清单；旧计划和旧交接文档中的待办表述，以本文为准。
> 本轮目标：先整理和审核，不启动代码开发、官方升级或 profile 改动。用户确认后，再按阶段逐项执行并逐项回填日志。

## 一、先清理口径

以下事项已经在 C0050 完成，不再列为待办：

- E 阶段会话内并行与 UI：E1-E7。
- F1/F3/F4/F5：异步回传、Job Panel、Web 并发、多模态链路。
- G1/G2/G3：只读自动并行、写操作建议门、智能分流。
- H1 当前可用轨道回归，以及 H3 的部署、哈希和运行日志验证。
- P6.1 会话蒸馏、P6.1.1 审核中心和 AI 初审/修改追踪的代码、部署和专项验收。

旧文档中“E/F1 待桌面实测”“F4/F5/G1 未开始”等内容均已被 C0050 取代。

## 二、排序后的执行清单

| 顺序 | 编号 | 事项 | 当前状态 | 启动条件 | 完成条件 |
|---|---|---|---|---|---|
| 0 | PLAN-APPROVAL | 审核本清单并确认顺序/范围 | 待用户审核 | 用户确认 | 形成明确的执行批次；未确认项不执行 |
| 1 | P6.1.1-USER | 在知识库“审核”页处理当前 8 条 `01-Inbox` 草稿 | 待人工 | 用户逐条决定；AI 建议只作参考 | 每条有通过、退回、暂留或不采纳结果；发布前完成来源/预检/冲突确认 |
| 2 | C4 | R5 真实使用观察期 | 进行中，尚未收口 | 连续真实使用 3-7 天 | 有候选池增长、知识增长、审计样本、无回归记录；验收第 7 条有证据 |
| 3 | G4 | 路由数据观察与校准准备 | 观测已启动 | 与 C4 并行积累真实任务 | 滚动 14 天；至少 20 条路由任务、10 次人工验收；先出分析报告，再决定是否调阈值 |
| 4 | D1 | `signal timed out` 最终桌面确认 | 待确认 | 有可用桌面环境 | 连续打开 20 个历史会话无超时；记录版本、会话数和结果 |
| 5 | C5 | R5/S5 最终交付收口 | 未开始 | C4 完成且 C1-C3 证据仍有效 | 验收 8 条全部有证据，交付清单 S0-S5 标记完成 |
| 6 | H3-DISTILL | 大阶段蒸馏与交付归档 | 延后 | C5 完成，且 G4 已有观察结论 | R3-R5 与 E-H 的可复用经验进入 `01-Inbox`；计划、交接、日志一致 |
| 7 | D3 | 朋友机器 profile 重置后验证 | 待验证 | 有测试机器和安装包 | 覆盖安装、启动、基本会话和工作台入口均正常；保留结果记录 |
| 8 | F2 | 双轨 provider：Codex/Claude Code Bundle | 暂停，官方兼容依赖 | 官方 provider 与核心 peer 版本兼容；先备份 profile | 只读试点可运行、状态/错误/成本边界明确；失败安全回退轨道 A |
| 9 | F6 | 写型子代理 worktree 隔离 | 暂停，官方能力依赖 | 官方支持 per-request `cwd` 和完整工具组合，或 F2 提供可审计隔离 | 写代理在独立工作树运行；父目录不被写入；停止/失败/清理可验证 |
| 10 | H2 | 官方升级兼容验证 | 暂停，官方发行版依赖 | 出现可安全升级版本；先备份 `harness-home` | SQLite 迁移、Job Panel、Bundle、多实例 Codex 均通过回归 |
| 11 | EXPERT-DESIGN | 工作台专家完整设计 | 独立待讨论 | 用户另行确认画像、人格、工具权限边界 | 只交付方案文档，不改专家配置，不自动写入用户画像 |
| 12 | P6.2 | 用户画像 | 暂缓 | 专家设计和权限边界先确认 | 画像字段、用户授权、更新/撤销和隔离规则经单独审核 |
| 13 | D4 | 独立 `dsh-workbench` 仓库依赖对齐 rc.2 | 未开始，低优先 | 主线观察期不再引入新变量 | 独立仓库依赖与 `plugins/workbench` 对齐并通过安装/冒烟 |
| 14 | D5 | 风格页半成品收尾 | 未开始，低优先 | 用户确认仍需要该页面 | V2 未完成部分补齐并完成页面回归 |
| 15 | D6 | 项目日志长期维护 | 持续 | 每次有改动或决策 | 按 AGENTS.md 追加 changelog、index、learnings、status |

## 三、建议执行批次

### 批次 A：当前可立即开始

1. 用户审核本清单。
2. 用户处理 8 条 Inbox 草稿。
3. C4 与 G4 同步观察；期间只记录数据，不自动调规则。
4. 有桌面条件时顺手完成 D1。

### 批次 B：观察期结束后收口

1. 汇总 C4 和 G4 数据。
2. 决定是否调整路由阈值；没有足够证据就保持现状。
3. 完成 C5/S5 验收。
4. 执行 H3 大阶段蒸馏和交付归档。

### 批次 C：外部条件满足后再启动

按 F2 → F6 → H2 的顺序评估官方兼容版本。任何一项不满足启动条件，都保持暂停，不用提示词或临时脚本伪造隔离能力。

### 批次 D：独立低优先事项

依次处理 D3、专家设计/P6.2、D4、D5。专家设计与用户画像不与知识审核或 provider 升级混在同一批次。

## 四、审核时请重点确认

- 是否同意先做“8 条 Inbox 人工审核 + C4/G4 观察”，而不是立即推进 F2/F6/H2。
- C5 是否要求等 G4 14 天数据一起收口，还是 C4 完成后先独立交付 R5。
- D1、D3 是否由本地桌面和朋友机器分别承担，还是暂缓到下一批次。
- 专家设计与 P6.2 是否继续保持独立，不纳入本轮执行。

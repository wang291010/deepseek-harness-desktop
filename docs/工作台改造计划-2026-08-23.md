# 工作台改造计划（2026-08-23 起，历史基线）

> **2026-08-25 更新：本文件保留阶段完成条件和历史证据；未执行项的实际顺序已迁移到 `docs/未执行计划重排-2026-08-25.md`，以 C0050 为准。**

> **2026-08-25 C0052：用户已确认恢复阶段 F/G/H 全部后续计划；当前执行顺序改用 `docs/阶段F-G-H恢复执行计划-2026-08-25.md`。F2/F6/H2 的实际状态以该恢复计划和 `.project-log/status.md` 为准。**

> 2026-08-24 用户优先级调整：阶段 F/G/H 后续推进暂时暂停，当前先完成“右侧工具栏会话蒸馏”的方案评审；方案见 `docs/会话蒸馏完整方案-2026-08-24.md`。功能须在用户审核方案后才实施。“工作台专家设计”登记为独立待办，另开讨论且只交付方案文档。

> 来源：`.project-log/status.md` 进行中/待办 + `docs/知识库改造交付清单-2026-08-22.md` 未完成项。
> 规则：每项带完成条件；A/B/C 阶段串行推进，D 类遗留可并行穿插；阶段完成时更新本项目计划与本日志。
> 蒸馏策略备注（2026-08-23）：用户倾向按大阶段蒸馏（如 R3-R5 视为一个阶段），本次维持现状，后续再议；在小阶段不再额外蒸馏。

---

## 阶段 A：R3 逐句 groundedness 收口（进行中，主体已由 C0017 完成）

| 项 | 内容 | 状态 | 完成条件 | 依赖 |
|---|---|---|---|---|
| A1 | 把 R3 改动部署到桌面应用安装目录 | ✅ 已完成（2026-08-23 20:56，C0019） | 源码与安装目录 SHA-256 一致；备份 backups/dsh-workbench-host-before-r3-20260823.js | 用户授权 AppData 写入 |
| A2 | 桌面内复验 hybrid 评分 | ✅ 已完成（C0020） | 桌面应用内 deep audit 返回 groundednessMethod=hybrid、rate=1 | A1 |
| A3 | 生成真实样本并过 R3 门禁 | ✅ 已完成（C0020） | 20/20 题审计，33 个逐句样本，groundedness=1.0（目标 ≥0.85） | A1 |
| A4 | 验收期正式切换 `auditLevel` 为 `ref+groundedness` | ✅ 已完成（C0020） | 应用配置已切换并持久化，后续编排收尾自动逐句审计 | A2、A3 |

## 阶段 B：R4 重排对比与评测补全（未开始）

| 项 | 内容 | 状态 | 完成条件 | 依赖 |
|---|---|---|---|---|
| B1 | 实现 cross-encoder 重排（`bge-reranker-v2-m3`），local/llm 已有 | ✅ 已完成（C0021） | 新增 knowledge_rerank.mjs（transformers.js + ONNX int8，570MB 已下载缓存）；`rerank: cross` 全链路可切换 | — |
| B2 | 三组对比：关闭 vs BGE 余弦 vs cross-encoder | ✅ 已完成（C0021） | 50 题实测：none recall=1.00/top1=0.78/648ms；local recall=0.98/top1=0.86/1120ms；cross recall=0.96/top1=0.84/2611ms（详见 docs/r4-rerank-compare-2026-08-23.json） | B1 |
| B3 | 重排默认值决策 | ✅ 已完成（C0021） | cross 无优势且慢 2.3 倍，不默认开启；local top1 提升 0.08 但 recall 降 0.02，默认保持 `none`（可手动切 local） | B2 |
| B4 | 评测指标补全：context relevance、completeness、faithfulness、真实 Web 次数、缓存命中 | ✅ 已完成（C0022） | 评测报告含 top1 命中率、avg top1 相关性、completeness（LLM 评估+重试）、faithfulness（=逐句 groundedness）、真实知识/Web 调用数（trace）、缓存命中率（自动检索统计）；实测 20 样本 completeness=0.871、groundedness=0.929 | — |
| B5 | 自动门禁：离线门禁 + 真实运行门禁，提供可失败退出的验收命令 | ✅ 已完成（C0022） | `run-knowledge-eval.mjs --gate`（离线：题数≥50、recall≥0.9、top1≥0.75、completeness≥0.8、faithfulness≥0.85）与 `gui-r3-groundedness-eval.mjs --gate-only`（真实运行：样本≥20、groundedness≥0.85、completeness≥0.8）均可失败退出 | B4 |

## 阶段 C：R5 最终交付与观察期（未开始）

| 项 | 内容 | 状态 | 完成条件 | 依赖 |
|---|---|---|---|---|
| C1 | 全量回归：全部 smoke + 50 题评测 + 桌面 GUI | ✅ 已完成（C0023） | 9 个进程内 smoke 全过；50 题离线门禁 recall=1.0 通过；真实运行门禁 groundedness=0.929/completeness=0.871 通过；知识库页 GUI 回归通过（检索/蒸馏/新建/总览）；重型对话/编排 GUI E2E 留待用户实测 | A、B |
| C2 | 源码与安装目录哈希一致 | ✅ 已完成（C0023） | 36 个部署文件 SHA-256 全量比对 0 差异（4 个缺失为开发文件不部署） | A1 |
| C3 | 验收 8 条逐条收口（1-4 已基本通过，5-8 补齐） | ✅ 7/8 收口（C0023），第 7 条待 C4 | 1-6、8 有证据；第 7 条（自生长/候选池增长）需观察期数据 | A、B、C1 |
| C4 | 3-7 天实际使用观察期 | 进行中（等待实际使用积累） | 观察候选池增长/知识增长/审计样本/无回归 | A4、C1 |
| C5 | 标记 S5 完成 | 未开始 | 全部门槛通过，交付清单 S0-S5 收尾 | C1-C4 |

## 阶段 D：其他遗留（可并行）

| 项 | 内容 | 状态 | 完成条件 |
|---|---|---|---|
| D1 | signal timed out 修复（C0003）真实桌面最终确认 | 待确认 | 连续打开 20 个历史会话无超时（用户实测） |
| D2 | 01-Inbox 经验人工审核移入 02-Atomic | 待人工 | 3 条草稿（C0002/C0003/本次蒸馏的 groundedness）审核后改 status=published 并移动 |
| D3 | 朋友机器 profile 重置后验证 | 待验证 | 覆盖安装启动正常 |
| D4 | 独立仓库 `dsh-workbench` 同步 rc.2 依赖 | 未开始（低优先） | 依赖与 `plugins/workbench` 对齐 |
| D5 | 风格页半成品收尾 | 未开始（用户已知） | 风格系统 V2 未完成部分补齐 |
| D6 | 项目日志长期维护 | 持续 | 每次改动按 AGENTS.md 补录 |

---

## 依赖顺序

```
A1（部署）→ A2/A3（复验+门禁）→ A4（切审计级别）
                    ↓
            B1 → B2 → B3 → B4 → B5
                    ↓
            C1 → C2 → C3 → C4 → C5

D1-D6 与主线并行，D2/D3 可随时人工处理。
```

## 验收 8 条对照（当前）

| # | 验收项 | 状态 | 收口条件 |
|---|---|---|---|
| 1 | AI 自行调用知识库并带引用 | ✅ 通过 | GUI 检索卡带引用；42 个样本全部含 [知识N] 引用 |
| 2 | 知识库+最新做法时主动联网并带 URL | ✅ 通过 | R2 trace 并行重叠证据 + webFallback 路由回归 |
| 3 | 缺知识不硬编，可联网或标"未验证" | ✅ 通过 | R3 abstain 口径：42 样本无编造，未验证句如实标注 |
| 4 | 寒暄 0 检索、0 额外 token | ✅ 通过 | gate 回归 + 自动检索统计 |
| 5 | 不污染上下文 | ✅ 通过 | 发送链路回归（C0013）保持 |
| 6 | 引用真实存在，无证据句被标注 | ✅ 通过 | 42 样本 invalid=0、groundedness=0.929 |
| 7 | 自生长：条目数/recall 上升、未命中进候选池 | ⏳ 待 C4 观察期 | 观察期数据（候选池 1 条起步、知识增长、审计样本累积） |
| 8 | recall@5≥0.9、groundedness≥0.85、abstain 可接受、成本可控 | ✅ 通过 | recall=1.0、groundedness=0.929、completeness=0.871、token -43%/延迟 -49%（R1 校准后） |

## 当前快照（2026-08-24 更新）

- 最近完成：R3 阶段 A 全部收口（C0017/C0019/C0020）；R4 阶段 B1-B5 全部完成（C0021/C0022）；阶段 C 的 C1-C3 完成（C0023），C4 观察期进行中；修复 C0024-C0027（未命中进候选池/任务状态锁/无会话兜底/失败透传）；多代理 E 阶段全部完成并部署（C0031-C0035：E1-E5 + 模型三档 + 失败自动换模型/单子代理重试 + 工具栏协作页签 + 删除状态胶囊）；全局 dsh CLI 升级至 0.1.1-rc.2（C0030）。
- 最近记录编号：C0039。
- 下一步：原阶段 F/G/H 后续计划按用户要求暂停；先评审会话蒸馏完整方案，审核通过后再制定 P6.1 执行清单并开发。工作台专家设计另开独立讨论，只出方案文档。C4 观察期不主动推进，保留既有数据。
- 风险提示：LLM 评估器（完整度/判定）存在偶发空返回，已加重试与数字兜底；`rerank: cross` 每查询约 2.6s，默认保持关闭；重型 GUI E2E（p26/p27/trace-e2e）会在应用内发消息/跑编排，建议用户实测时顺带验证；子代理默认超时已从 15 分钟降到 10 分钟，失败自动换模型最多 2 次。
- 测试修复（C0024）："未命中进候选池"判定增强——零结果 / 覆盖不足 / gray 且词面零覆盖（lexicalCoverage=0）都记为未命中，聊天与知识库页搜索均已接入；候选去重忽略空白差异。

---

## 阶段 E-H：多代理并行与对话化改造（2026-08-24 重规划）

> 触发：DeepSeek Harness 官方 RC.7/RC.8 更新推出子代理并行生态；工作台此前已具备"单 AI / 多 AI 切换"（P2.6）与"多AI协作编排"（P2.5），但两者割裂：并行子代理藏在"AI 协作"面板里，普通对话默认单 AI，主代理查看/修订要跳面板。
> 目标：把"子代理并行"变成普通对话的第一等能力——单/多 AI 开关即"是否允许并行子代理"的语义开关；AI 协作面板降级为详情抽屉；写操作保持人工确认，只读/检索类可自动并行。

### 官方更新依据（已核实）

- **RC.7（2026-08-17）**：Codex / Claude Code 子代理任务接入 Job Panel 统一管理。
- **RC.8（2026-08-19，共 14 项，与本改造相关）**：
  - 子代理收编：Claude Code、Codex 可作为 Profile Bundle 按需安装，在 Harness 工作流里充当具体任务执行者；
  - Codex 新增非交互权限模式 + 多个命名实例：一个任务可同时挂多个 Codex 干不同活（多实例并行）；
  - reportDelivery 机制：子代理结果及时回传并唤醒等待中的父任务，修正报告与任务结算通知乱序；
  - web_search 支持并发查询；
  - 多模态：/goal、/plan 支持图文混合输入；@ 菜单支持引用文件与历史会话（官方硬上限 3 个）；
  - Windows PTY 持久 PowerShell；SQLite 读写/分叉性能优化（数据结构不兼容，升级前需备份）。

### 版本基线（2026-08-24 核实）

- 应用内嵌核心 harness = `@deepseek-ai/dsh` 及全部核心包 **0.1.1-rc.2**（2026-08-23 更新安装，npm latest/next 同指此版本）。
- "rc.8"是 GitHub 开源仓库的发布标签 `v0.1.0-rc.8`（2026-08-19），与 npm 包版本线（0.1.1-rc.2）不是同一套编号；0.1.1-rc.2 是更晚的 npm 线，包含 rc.8 时代能力（Job Panel、reportDelivery、web_search 并发、多模态、子代理 bundle 支持），**核心不落后**。
- 注意：全局 `dsh` CLI（npm）仍是 0.1.0-rc.6（旧），管理 desktop profile 时应使用 `npx @deepseek-ai/dsh@0.1.1-rc.2` 或应用内嵌 CLI，避免用旧 CLI 改新 profile。
- 官方提示 SQLite 存储格式不兼容：升级/动 profile 前备份 `harness-home`。
- 阶段 F 的"官方新能力"一律以安装包实际能力验证为准，不照搬媒体对 rc.8 的描述。

### 工作台现状（与官方能力对照）

| 能力 | 工作台现状 | 与官方更新差距 |
|---|---|---|
| 单/多 AI 切换 | 会话侧栏 ChatModeSwitch（按会话记忆）+ 多 AI 对话壳（发送→创建编排→实测→方案→自动启动→对话流浮层） | 多 AI 仍走"创建任务→方案→执行"重链路；快速问答也建编排任务，未直发主会话 |
| 并行子代理 | AI 协作页编排：依赖拓扑、maxParallel≤4、watchdog 重试、SSE 进度 | 并行只发生在编排任务内，普通对话不自动并行 |
| 子代理 provider | 单一 `subagents.start('spawn')`（in-process） | 未接入 Codex / Claude Code Profile Bundle 与多命名实例 |
| 父任务唤醒 | 主代理 start 收集语义，等整批子代理结束后才汇总 | 未利用 reportDelivery"完成即唤醒父任务" |
| web_search | 知识检索 webFallback 单查询 | 未接入并发查询 |
| @ 引用 | 想法 / 项目文件 | 未支持历史会话引用 |
| 多模态 | 未验证 | /goal、/plan 图文输入链路未接入工作台 UI |

### 阶段 E：会话内并行（对话即工作台）—— ✅ 已完成（C0031-C0035）

| 项 | 内容 | 状态 | 完成条件 |
|---|---|---|---|
| E1 | 单/多 AI 开关语义化：单 AI 复杂问题直发原生会话；多 AI = 允许并行子代理；快速问答不再建编排任务 | ✅ 已完成（C0031） | 快速问答经 session.prompt 直发原生、零编排记录；"始终编排"策略保留 |
| E2 | 对话流并行卡片：默认一行摘要（N 个子代理并行中 · M/N 完成 · 已用时间），点击展开子代理列表（状态/模型/耗时/失败原因），完成后自动折叠为主代理汇总卡 | ✅ 已完成（C0031/C0033） | 一次复杂任务全程不离开对话流可完成并修订；卡片默认折叠不刷屏 |
| E3 | 写操作确认门：并行子代理默认只读（host readOnly + 方案生成提示词契约 C0032）；含写操作子代理时自动启动前出确认卡（仅读执行/允许写操作）；审批浮层（允许一次/拒绝，最佳努力） | ✅ 已完成（C0031/C0032） | 写操作默认确认；冒烟覆盖"并行只读"与"写操作需确认"；AI 协作页保留 |
| E4 | 新增"对话流详情抽屉"（实验）：从对话流卡片点开右侧抽屉（方案/代理状态/日志/记忆/决策）；**AI 协作整页保留不动** | ✅ 已完成（C0031） | 抽屉可从对话流卡片打开并返回；AI 协作页无回归；效果不好可回退 |
| E5 | 成本与并发护栏：并行上限默认 3（可调 1-4）；卡片显示预计 LLM 调用与 token 量级；运行中可停止、失败后可继续执行 | ✅ 已完成（C0031/C0034） | 冒烟覆盖上限/停止/恢复；成本信息可见 |
| E6 | 补充（用户实测反馈）：子代理运行情况移入右侧工具栏"协作"页签，对话区零浮层（C0033/C0035） | ✅ 已完成 | 实时状态在工具栏常驻可看；右上角不再与用量详情重叠 |
| E7 | 补充（用户实测反馈）：模型分配三档（全自动/主代理手动·子代理自动/全手动）+ 失败自动换模型重跑 + 单子代理重试 + 常驻停止按钮 | ✅ 已完成（C0034） | 对话流/协作页均可选策略；失败子代理可单独重试；smoke-worker-failover 通过 |

### 阶段 F：并行引擎升级（接官方 RC.8）

| 项 | 内容 | 状态 | 完成条件 |
|---|---|---|---|
| F1 | 轨道 A 异步回传：worker 改用官方 continuable 子代理，支持 reportDelivery/结束通知唤醒父任务；调度器改为动态并行槽，不等整批；旧运行时自动回退 one-shot | ✅ 完成，真实桌面 E2E 通过 | `smoke-orchestration-delivery.mjs` 与真实 GUI 证明依赖快任务的 worker 早于无关慢任务结束即启动；worker 记录 continuable/回传时间 |
| F2 | 双轨 provider：轨道 A（默认）= harness 同进程子代理（状态/日志/重试/模型分配完整、可审计）；轨道 B（按需）= Codex / Claude Code Profile Bundle（独立产品进程、只回最终文本）；Codex 多命名实例可选 | ⏸ 等待官方兼容版本 | 当前 0.1.1-rc.2 未内置 Codex/Claude provider，公开 provider 包与当前核心 peer 版本不兼容；G3 会显示建议 B、实际安全回退 A |
| F3 | Job Panel 统一可见性：子代理任务在官方 Job Panel 可查，与工作台任务库双向可见 | ✅ 完成，真实桌面 E2E 通过 | 编排注册为会话归属的 `orchestration-*` 官方任务；官方 jobs/model kill 与工作台停止共用控制器；任务库保存 jobId；实测面板运行态出现、工作台停止后更新 cancelled。rc.2 人类面板按官方设计只读，无停止按钮 |
| F4 | web_search 并发：知识检索 webFallback 多问题并发查询 | ✅ 完成，真实桌面 E2E 通过 | 三路 Web 查询同批发起，trace 有并行重叠证据；输出 Schema 已兼容 rc.2 子集 |
| F5 | 多模态链路验证：/goal、/plan 图文混合输入在对话流/编排可用（模型需支持） | ✅ 完成，真实桌面 E2E 通过 | 官方附件存储、图片引用、视觉证据提取及规划模型回退已打通 |
| F6 | git worktree 隔离：写型子代理可选独立工作树 | ⏸ 等待官方能力 | 当前同进程子代理继承父 cwd 且没有单次 cwd 覆盖；已撤回无真实工具隔离的试验实现，等待官方 per-request cwd + 完整工具组合或 F2 可用 |

### 阶段 G：自动并行（普通对话自动拆解）

| 项 | 内容 | 状态 | 完成条件 |
|---|---|---|---|
| G1 | 只读/检索类自动并行：默认开启，普通消息含多个可拆解的只读子问题时，主代理自动并行拆给子代理，对话流展示"已并行 N 个子代理" | ✅ 完成，真实桌面 E2E 通过 | 默认开、可一键关闭；三问题拆为三只读子代理且有真实并行重叠证据 |
| G2 | 写操作建议卡：检测到写操作时出"建议并行"卡片，确认后才执行 | ✅ 完成 | 写操作只展示建议，不自动启动；用户选择仅读或明确允许写操作 |
| G3 | 智能分流：按任务价值、可并行度、延迟收益与写意图选择 quick/parallel/orchestrate、1-4 个代理及 A/B 轨道 | ✅ 完成 | 8 类路由样例通过；B 不可用时保留建议证据并安全回退 A；UI 展示规模与回退原因 |
| G4 | 数据驱动调优：按通过率/返修率/失败率/耗时调整阈值与默认策略，观察 1-2 周 | ⏳ 观测已启动 | 滚动 14 天指标已落地；至少 20 条路由任务且 10 次人工验收后再校准，当前不自动改规则 |

### 阶段 H：回归与收尾

| 项 | 内容 | 状态 | 完成条件 |
|---|---|---|---|
| H1 | smoke + 编排 E2E + 并行正确性（并发上限、乱序、失败重试、唤醒及时性） | ✅ 当前可用轨道回归完成 | 15 组 smoke、check 及 F1/F3/F4/F5/G1 真实专项 E2E 通过；双轨混跑随 F2 延后 |
| H2 | 官方升级兼容验证：SQLite 迁移备份、Job Panel、多实例 Codex 实测 | ⏸ 等待兼容发行版 | 当前 npm 最新仍为 0.1.1-rc.2，暂无可安全升级并完成 F2/F6 的官方组合 |
| H3 | 部署 + SHA-256 + 日志补录 + 大阶段蒸馏 | ⏳ 部署/哈希/日志完成，蒸馏延后 | 普通模式重启、源码/安装目录 SHA-256 一致、运行日志确认知识工具注册；F/G 尚有依赖项和观察期，按“大阶段完成再蒸馏”策略暂不提前入库 |

### 依赖顺序

```
E1 → E2 → E3 → E4
        ↓
      F1（异步回传）→ F3（Job Panel）→ F2（双轨 provider 试点）
        ↓
      F3/F4/F5 → G1/G2 → G3 → H1-H3
```

### 与旧计划的衔接

- A-D 阶段已收口或属遗留并行项（D1-D6 不变）。
- 之前的讨论结论并入本规划：写操作默认确认（E3/G2）；AI 协作面板降级为详情抽屉（E4）；自动并行先只做只读/检索（G1）；默认半自动气泡起步（E2），全自动（G）等数据。
- E4 澄清（2026-08-24）：不删 AI 协作整页；"详情抽屉"只是从对话流新增的快捷查看入口，先做出来看效果，可回退。
- 已确认的默认值（2026-08-24，用户按推荐确认）：E3 写操作默认确认=开；G1 只读自动并行默认=开；F2 轨道 B（Codex/Claude Code bundle）先不装、等 E/F 主链路跑通再试点；详情抽屉按实验实现可回退；并行上限默认 3。

### 阶段 F/G/H 状态（2026-08-24 更新）

- **阶段 F（主链路完成，兼容项暂停）**：F1/F3/F4/F5 已完成真实桌面验证；F2 等官方兼容 provider，F6 等官方单次 cwd/完整工具能力。
- **阶段 G（功能完成，进入观察）**：G1/G2/G3 已完成；G4 已开始滚动 14 天采样，达到 20 条任务 + 10 次人工验收后再校准。
- **阶段 H（当前版本收尾完成）**：H1 当前可用轨道全量回归通过；H2 等官方兼容发行版；H3 已部署、哈希和运行日志验证，因 F/G 大阶段尚有依赖项/观察期而延后蒸馏。

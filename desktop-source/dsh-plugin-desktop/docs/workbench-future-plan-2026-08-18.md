# DeepSeek Harness 桌面工作台 — 未来完整计划（2026-08-18）

> 本文档是后续所有工作的唯一执行依据。变更计划需先更新本文档并经确认。
> 基础事实见 [workbench-build-plan-summary-2026-08-18.md](workbench-build-plan-summary-2026-08-18.md)。

## 0. 计划定位与执行原则

### 总目标

把 DeepSeek Harness Desktop（B 端）持续打磨成个人 AI 工作台：六页能力（Agent /
知识库 / 专家 / 风格 / 监控 / 工作流）全部可用、任务中心与 AI 协作成为日常核心，
同时保持"稳定可发布、可回滚、可自动更新"的桌面发行基线。

### 执行原则

1. 板块化推进，一次只动一个板块；每个板块按固定循环：现状核验 → 方案（含决策点）→
   备份 → 实施 → 回归验收 → 更新文档 → 提交。
2. 验收通过才算完成；未验收的功能一律标记为"已开发，待回归"，不算正式完成。
3. 任何改动先备份；Client 改动刷新即生效，Host 路由/命令与专家 preset 改动需重启
   B 端；B 端故障不影响 A 端；A/B 不同时打开同一会话。
4. 安全约束持续生效：文件接口仅本机回环、发布前敏感审计、profile JSON 无 BOM、
   旧 DSH 数据不迁移不删除（除非决策清单明确）。
5. 每阶段结束时可发布一个版本；版本发布走既定 SOP（见 Phase 7）。

## 1. 总路线图

| 阶段 | 名称 | 目标 | 前置 | 状态 |
|---|---|---|---|---|
| P0 | 文档与基线对齐 | 交接文档 + 基线文档更新并提交 | 无 | ✅ 完成（53fd34e） |
| P1A | 任务面板重构与体验升级 | 按用户反馈 + 市面方案重做任务中心，修复全部已知 bug | P0 | 🔄 已实现并部署（71e3479），待用户真机复测 |
| P1B | 其余运行端回归 | 文件视图 / 外壳 / 专家页全部验收通过 | P1A | 🔄 代码项完成（5fc2345），GUI 回归待用户实测 |
| P2 | 风格页 | 视觉与对话风格可配置、持久化 | P1 | ✅ 已完成并部署（b8eb587），GUI 回归通过 |
| P2.5 | 多AI协作工作台（主会话驱动） | 主会话持续对话 + 任务自动拆解编排 + 状态可视化 + 记忆快照 | P1A/P2 | ✅ 已完成并部署（2.5-A/B/C） |
| P2.6 | 对话窗口多AI模式 | 普通对话窗口单/多AI开关 + 主代理编排 + 自动测模型分配 + 对话流卡片 + 引用浮层 + 项目上下文设置 | P2.5 | ✅ 已完成并部署（852929f/cabc9f0/f87debd/0dc4b28，GUI 通过） |
| P2.7 | 对话与项目配置体验 | 模式切换入侧栏 + 协作任务进看板/复盘 + 项目配置独立工具栏栏 | P2.6 | ✅ 已完成并部署（19e7e6b/f798812，GUI 通过） |
| P3 | 监控页 | 账户/用量/会话/实时/告警五板块 | P2 | ✅ 已完成并部署（P3） |
| P4 | 工作流页 | 模板库 + 一键运行 + 定时调度 | P3 | ✅ 已完成并部署（P4） |
| P5 | 知识库与蒸馏 | 本地 Markdown Vault（Obsidian）+ 蒸馏入库 + 多路检索 + 自生长维护 | P4 | ✅ 已完成并部署（v3.1，2026-08-19） |
| P6 | 打磨与文档 | 视觉统一、性能、安全复查、全套文档 | P5 | 待执行 |
| P7 | 发布链路完善 | 签名、安装怪癖、SOP、跨平台、迁移、便携版 | 可与 P2–P6 并行 | 部分待执行 |

## 2. 各阶段详细计划

### Phase 0：文档与基线对齐（预计半天）

任务：

1. 把上一份计划总结补全为正式《工作台交接文档》（含本计划、两个会话成果、约束清单）。
2. 更新 `docs/version-baseline.md`：桌面 2.0.1、自动更新已启用、插件与工具版本、
   最新安装包哈希。
3. 更新 `docs/desktop-installer-smoke-2026-08-18.md` 或另建 2.0.1 记录（如必要）。
4. 提交。

验收：新会话仅凭仓库 + 交接文档即可无缝续作；基线文档与当前实际完全一致。

### Phase 1A：任务面板重构与体验升级（最高优先，用户反馈驱动）

#### 背景

用户集中反馈任务面板存在体验和稳定性问题，并要求先参考市面成熟方案再做。
本节把反馈、代码根因、市场对标和重构方案固定下来；重构完成并验收通过后，
才进入 Phase 1B 的其余回归。

#### 1A.1 用户反馈与代码根因

| # | 用户反馈 | 代码根因（已定位） | 修复方向 |
|---|---|---|---|
| 1 | 新建项目只能输入路径，不能选文件夹 | 会话栏"创建新项目"是纯文本输入（`SessionPanel` 的 `wsPath`） | 增加"浏览…"按钮；用 `<input type="file webkitdirectory>` 选取文件夹，Electron 下用 `File.path` 还原绝对路径；原生系统对话框列为可选增强（需桌面 preload IPC） |
| 2 | 想直接在 AI 协作页发任务，不必先进想法库 | 新版三栏 `WorkbenchOrchestration` 没有新建输入框（旧版有，未启用）；只有"去想法库挑选" | 在 AI 协作页顶部加 Quick-Add 输入（Todoist/Linear 风格）：回车即创建协作任务，可带项目与简要说明，创建后自动选中并可直接生成方案 |
| 3 | 想法库"交给 AI 团队"后 AI 协作页空白；过一会关闭重开才好 | `idea_convert_orchestration` 后跳转 `orchestrate` 时，`viewProjectPath`/`scope` 过滤不匹配新任务（如全局想法），`items` 为空且无提示；`selected` 找不到就显示误导性空态 | 转换后自动把任务中心范围切到该任务的项目并选中它；`WorkbenchOrchestration` 优先按 `initialId` 全局查找，找不到才用范围过滤；空态文案区分"真的没有任务"和"任务被过滤/尚未加载" |
| 4 | 生成/重新生成方案时无进度，执行中看不到任务进度 | `orchestration_plan` 在 mutate 请求内同步跑 LLM（可能 1–3 分钟），期间没有任何落盘状态，客户端 `busy` 无展示；执行中只有 1.8s 轮询刷新，无进度可视化 | 见 1A.3"状态机与进度可视化" |
| 5 | 确认方案点开始任务后，项目切换按钮失灵 | 需复现确认；候选：运行期间任务中心轮询/错误表面干扰、`busy` 未复位、或全局错误遮罩挡住交互 | 复现并修复；回归保证运行中项目切换、开关任务中心、切换 tab 全部可用；所有失败路径复位 `busy`；错误表面改为不遮挡交互（`pointer-events` 仅自身可点） |
| 6 | 右侧反馈框内容关闭再打开不重置 | 已提交的反馈被持久化到 `item.feedback`，重开时重新填入；草稿与已提交反馈未分离 | 草稿只存本地组件状态；提交后立即清空；已提交反馈只出现在"历史"tab 的修改依据中，不再回填输入框 |
| 7 | 左侧红色报错 "unexpected end of json input"，且无法复制报错 | `fetch(...).then(r => r.json())` 在响应体为空时抛该错；全局错误表面只有文本+点击清除，无复制按钮；`navigator.clipboard` 在部分环境不可用 | 统一 fetch 包装：读文本→容错解析→带超时；所有错误提示（store.err、全局错误条、开发错误遮罩、编排错误）加"复制"按钮，复制用 `execCommand('copy')` 兜底；全局错误条可一键复制并不可遮挡交互 |

#### 1A.2 市场对标（本次任务面板的设计参照）

- **Linear**：键盘优先、Cmd-K 全局入口、极快响应、最小化；适合个人工作台的轻量感。
- **Todoist Quick Add**：任意位置一键建任务、自然语言解析项目/优先级/日期；适合"在 AI 协作页直接发任务"。
- **GitHub Actions / CI 运行视图**：一次运行 = 阶段步骤列表，每步 running / completed / failed 徽标 + 日志 + 耗时；是"AI 团队执行进度"最直观的参照。
- **LangChain DeepAgents / agent-teams-dashboard**：实时展示子代理流、每个子代理独立进度卡、可展开对话与工具调用统计；适合本工作台的逐代理进度卡。
- **Claude Code Agent Monitor**：Kanban 状态板 + 会话/代理/工具实时活动 + 事件驱动更新（对比当前 1.8s 轮询，后续可升级 SSE/WebSocket）。
- 结论：任务面板以"线性列表 + 运行视图 + 快速创建"为核心，不堆企业级字段；AI 协作采用 CI 式运行页，规划阶段用不确定进度条，执行阶段用逐代理状态卡。

#### 1A.3 状态机与进度可视化设计

规划阶段：

- Host 在 `orchestration_plan` 开始时先把记录置为 `planning` 并落盘，生成完成后再写 `planned` + 方案版本；请求立即返回，生成在后台队列执行，客户端轮询刷新。
- 规划中 UI：不确定进度条 + 文案（"AI 正在生成第一份方案…"/"正在按反馈重新编排 V2…"）+ 已用时长；方案版本区显示"V1 生成中"。
- 崩溃/重启恢复：`recoverInterruptedOrchestrations` 把卡在 `planning` 的记录标记为失败并允许重新生成（该函数已存在，落地 `planning` 后即可生效）。

执行阶段：

- 顶部整体进度条：`已完成子代理 / 子代理总数`，主代理运行后进入"主代理汇总中"。
- 逐代理状态卡：planned / waiting / running / completed / failed / cancelled 徽标；运行中的代理显示已用时长；完成代理显示交接摘要（可展开）。
- 阶段步骤条：想法 → 方案 → 执行 → 验收，高亮当前阶段。
- 执行时间线：开始、每批子代理完成、主代理汇总、进入验收；失败与取消带原因。
- 数据已具备：Host 已在每个 worker/main 的状态、`startedAt`/`completedAt`、`output`/`error` 落盘；客户端只需按上述 UI 消费。

#### 1A.4 重构实施清单（小步、每步可验收）

1. 错误与网络层：fetch 统一包装（超时、空响应容错、错误信息可读）；错误表面可复制、可关闭、不挡交互；所有失败路径复位 `busy`。
2. 快速创建：AI 协作页顶部 Quick-Add（直接 `orchestration_create`），创建后选中并保持项目范围。
3. 转交修复：想法库 → AI 协作跳转自动修正范围并选中新任务；空态文案区分原因。
4. 规划异步化 + `planning` 落盘 + 规划进度条（首次生成与重新生成都覆盖）。
5. 执行进度可视化：整体进度条、逐代理状态卡、阶段步骤条、执行时间线。
6. 反馈草稿与已提交反馈分离；提交后清空；历史 tab 展示修改依据。
7. 项目创建增加"浏览…"选文件夹；路径输入保留。
8. 运行中交互回归：项目切换、会话操作、任务中心开关、tab 切换全部可用；修复确认的根因。
9. 双客户端同会话风险提示（沿用 P1 待办，一并收口）。
10. 中断/异常任务的“继续执行”：保留已完成代理的产出，只重跑未完成代理后由主代理汇总；
    失败原因可复制诊断；无法解决再回退“重新生成方案”（2026-08-18 追加）。

#### 1A.5 验收标准

- 上述 7 个用户反馈场景逐一复测通过（含真实执行一轮小任务）。
- 规划中与执行中均有明确进度反馈；无空白无进度时段。
- 任何错误都可一键复制；错误提示不阻塞其他操作。
- 运行期间项目切换等全局操作不受影响。
- 关闭重开任务中心后，反馈框为空白（未提交草稿不残留）。
- 项目可直接选文件夹创建。

### Phase 1B：其余运行端回归

#### 1B.1 任务中心存量回归（P2+）

回归清单：

- 看板：添加、编辑、删除；拖拽改状态；勾选完成/取消；搜索过滤；排序；完成率与进度条。
- 目标：显示当前目标与阶段/轮次；暂停、恢复、编辑、清除。
- 写入链路：`/todo replace` 整表写入、`todo/write` 领域事件落盘；新对话轮次开始后的
  任务状态行为；模型重写与手动修改的覆盖行为（决策点 D1）。
- 想法库：快速记录 → 收件箱 → 评估（AI 按需整理）→ 稍后 / 推进 / 归档；转普通任务、
  转 AI 协作任务；随项目绑定与"打开想法库"跳转。
- AI 协作：方案 V1/V2 版本、执行轮次、执行前反馈重编排、执行后验收或要求修改；
  主代理与每个子代理的模型选择、模型白名单校验、不可用回退与历史记录。
- 数据层：任务库版本升级、空状态、异常状态。

修复项（视回归结果）：

- 任务模型扩展：截止日期、标签、优先级、负责人（决策点 D1）。
- 若决定"模型不得覆盖手动任务"：建立独立任务数据层，与 `/todo` 同步但以手动为准。

验收标准：清单内每项在真实桌面端操作通过；临时测试数据清理；重启后数据保留；
任务库结构与 README 一致。

#### 1B.2 文件视图 + 安全收口

回归清单：

- Windows 盘符面包屑、目录切换、文件打开、编辑保存、512KB 大文件提示、
  无权限/路径错误提示。

安全收口：

- 文件接口增加允许目录限制：默认只允许当前项目目录（或用户配置的根目录），
  拒绝越界绝对路径；仅本机回环监听（维持现状）。

验收标准：以上交互全部通过；越界路径返回明确错误且不产生文件写入。

#### 1B.3 外壳与导航收尾（P1）

回归清单：

- 项目切换记忆（重启后保持上次项目）。
- 会话置顶。
- 会话面板拖拽调整宽度。
- 插件商店与设置按钮样式统一。
- 重命名、归档完整回归。
- 双客户端打开同一会话的风险提示。
- 默认收起布局回归：左栏图标窄栏、右栏默认关闭、手动展开、本次使用状态保持。

验收标准：P1 清单全部通过，无白屏；错误边界与安全模式仍可用。

#### 1B.4 专家页（P2）

回归清单：

- 调用专家创建新会话；人格正确生效。
- 表单编辑（人格/技能/工具）并保存；复制；删除。
- Host 端 preset 文件读写；AI 辅助修改建议接口。
- 重启 B 端后 preset 重新加载。

已知限制记录（不阻塞，但写入 README）：

- 已产生内容的会话不能中途切换专家。
- 专家修改后可能需要重启 B 端生效。
- 专家权限等于其引用插件的权限。

验收标准：两位专家（健身专家、AI 产品架构师）均可创建会话；四个操作按钮可用；
出错提示明确且不破坏原 preset。

### Phase 2：风格页（P3）

功能清单：

- 主题模式（亮色/暗色/跟随系统）；强调色；壁纸；背景透明度；暗色遮罩；
  毛玻璃程度；字体大小；圆角；界面密度。
- 风格预设保存、一键切换整套风格、设置持久化、实时预览。
- 对话风格层：与专家分离（专家决定"你是谁"，风格决定"怎么表达"）；MVP 全局默认，
  可叠加任意专家；后续再按会话保存。

验收标准：视觉设置实时生效；重启后保持；预设可保存/切换；对话风格能影响回答方式。

完成记录（2026-08-19，b8eb587）：

- 全部功能清单已实现；Host 新增 `GET /style/read`、`POST /style/write`，设置持久化到
  `DSH_HOME/dsh-workbench-style.json`（串行队列 + 原子替换，revision 递增）。
- 验收已通过：外层源码 smoke ×3、eslint、`node --check`、tsc ×4 全过；桌面端部署并重启后
  API 往返验证通过；CDP 真实桌面 GUI 回归覆盖外观（主题/强调色/滑块/密度）、壁纸上传移除、
  对话风格（简洁/自定义）、预设（内置应用/自定义保存/删除）、恢复默认，全部通过并回到默认状态。
- 遗留说明：GUI 回归期间桌面端以 `--remote-debugging-port=9224` 运行；本轮 API 端口
  `127.0.0.1:58915`（每次重启会变）。对话风格为全局 MVP，按会话保存留待后续迭代。

### Phase 2.5：多AI协作工作台（主会话驱动）（P2.5）

> 需求来源：用户与其他 AI 讨论后提出的改造方案（2026-08-19），目标是让"任务中心-想法库-
> AI 协助"演进为**主会话驱动的多AI协作工作台**：像聊天一样持续对话、复杂任务自动拆解给
> 多个专业子代理、状态实时可视化、上下文可跨会话恢复。

#### 现状核对（与方案的重叠部分，已有能力）

方案中的"多AI协作引擎"在 P1A 已实现大半，无需重复建设：

- 任务拆解与编排：`orchestration_create / set_planning / plan / set_plan / start / accept /
  request_changes / cancel / remove`；主协调代理 + 至多 6 个子代理、`dependsOn` 依赖、
  `maxParallel`（1–4）并行上限、plan 版本与 runs 历史、`thread` 对话记录。
- 执行与监控：逐代理状态卡（planned/waiting/running/completed/failed/cancelled）、
  阶段步骤条、整体进度、主代理汇总、运行中可终止、验收后可"继续与主代理对话"（918d32c）。
- 中断恢复：`recoverInterruptedOrchestrations` 保留已完成代理产出只重跑未完成代理；
  模型选择与白名单（V4 Flash / V4 Pro）、不可用回退。

#### 真正的新增工作（方案增量）

按模块拆分：

- A 会话窗口改造：统一会话入口（想法库改为会话内快捷引用抽屉）、普通/多AI 模式切换开关、
  子任务折叠卡片内嵌消息流、@引用与拖拽上传、快捷命令（`/new` `/plan` `/memory`）。
- B 协作引擎增强：复杂度自动判断（启发式规则 + LLM JSON，阈值可配）、子代理池配置化
  （角色/能力标签/模型/系统提示词 + 匹配）、看门狗（单任务超时默认 300s、重试 ≤2 次、
  换同类代理、全失败标记"需人工介入"）。
- C 右侧状态面板：概览（进行中/已完成/进度/队列/预计完成）、代理状态、执行日志时间线
  （搜索过滤）、记忆快照 4 个 Tab。
- D 项目上下文：项目工作区（名称/描述/文件列表/环境变量/技术栈）、上下文自动注入
  （文件结构摘要、历史摘要、相关快照）、文件上传分发与大文件摘要。
- 记忆快照（核心新增）：子代理执行完生成压缩快照（执行上下文/关键发现/决策依据/改动摘要），
  持久化存储，Token 恢复跨会话上下文。

#### 实施阶段（每阶段走固定循环：现状核验 → 方案 → 备份 → 实施 → 回归 → 文档 → 提交）

阶段 2.5-A（MVP，让协作"能用"）：

1. 会话线程化：AI 协作页演进为主会话视图，编排记录以聊天消息 + 折叠卡片呈现，
   普通/多AI 模式开关（MVP 默认手动切换，不自动触发）。
2. 复杂度判断：先做可配置启发式规则 + 可选 LLM JSON 判定（默认关闭自动启动）。
3. 看门狗 v1：子代理超时（默认 300s）→ 重试 ≤2 次 → 换同类代理 → 全失败标记需人工介入；
   Host 新增超时控制与状态字段。
4. 右面板 v1：概览 + 代理状态两个 Tab（复用现有逐代理状态卡数据）。

阶段 2.5-B（体验优化，让协作"好用"）：

5. 执行日志 Tab（时间线、级别配色、按任务过滤与搜索）。
6. 输入增强：@引用想法库/历史消息/项目文件；拖拽上传文件（Host 回环 + 工作区限制 +
   大小上限，文本大文件自动摘要）；快捷命令。
7. 子代理池配置化：Host 端 `agents.json` 风格配置（名称/角色/模型/能力标签/系统提示词），
   拆解提示词携带可用代理列表做匹配。
8. 编排可视化：任务依赖图/流水线视图（串行/并行关系）。

阶段 2.5-C（完整形态，让协作"强大"）：

9. 记忆快照：生成、持久化、记忆库 Tab、复制/加载 Token（注入当前会话上下文）；
   快照字段与大小上限见 D16。
10. 项目工作区与上下文自动注入：文件结构摘要、技术栈、历史摘要、相关快照。
11. 成本护栏：全局并行代理上限、任务/每日 Token 预算提示；新旧界面切换保留任务中心
    作为历史与管理视图。

#### 验收标准

- 普通模式问答与当前行为一致；多AI模式下复杂任务自动出计划、执行、汇总，主会话可继续追问
  单条子任务而不重跑其他结果。
- 子代理状态与日志在右侧面板实时可见；超时重试/换代理/需人工介入路径可复现。
- 拖拽上传与 @引用可用；文件不越出注册工作区、仅本机回环。
- 记忆快照可生成、持久化、跨会话恢复；Token 加载后主代理能引用历史结论。
- 回归通过后按既有流程部署（备份 → 复制两文件 → 重启 → 定位端口 → CDP 回归）并提交。

2.5-A 完成记录（2026-08-19）：

- Host 看门狗：子代理超时（默认 300s，`DSH_WORKBENCH_WORKER_TIMEOUT_MS` 可调）→ 自动重试
  （≤2 次，`DSH_WORKBENCH_WORKER_MAX_RETRIES`）→ 全部失败时编排进入"执行异常"并提示
  需要人工介入（跳过主代理，节省成本）；代理记录新增 `attempts` 字段。
- Host 快速问答标记：`orchestration_create` 支持 `quick`，规划提示词注入"快速问答模式"
  （单个直接回答代理，不做拆解）。
- 客户端：协作模式开关（快速问答 / 多AI协作，localStorage 持久化）、复杂度启发式判断
  （阈值 0.6，"自动判断复杂任务"默认关闭，开启后按复杂度双向切换模式）、输入区复杂度徽标、
  代理卡显示尝试次数、右侧决策面板升级为 概览 / 代理状态 / 决策 三 Tab（空态提示）。
- 验证：`smoke-watchdog.mjs` 新增并通过（超时重试 3 次、需人工介入、quick 标记与提示词）；
  smoke ×4、eslint、`node --check`、tsc ×4 通过；CDP GUI 回归通过（模式开关/徽标/自动判断/
  快速问答创建/三 Tab 面板/空态），截图 `collab-compose.png`、`collab-panel.png`。
- 部署：备份 `backups\workbench-p2.5-mvp-deploy-20260819-100625`，运行副本哈希与源码一致，
  桌面端已重启（本轮 API 端口 63470，带 `--remote-debugging-port=9224`）。

2.5-B 待办：执行日志 Tab（时间线/级别/过滤）、@引用与拖拽上传（Host 回环 + 工作区限制 +
  大小上限 + 大文件摘要）、快捷命令、子代理池配置化、编排依赖可视化。

2.5-B 完成记录（2026-08-19）：

- 执行日志：编排记录新增 `log` 时间线（info/warn/error，上限 200 条），覆盖子代理执行/重试/
  超时/完成、主代理汇总、优化轮次与终止路径；右侧面板新增"日志"Tab（级别过滤 + 文本搜索）。
- 附件：`POST /api/dsh-workbench/attachment/put`（仅本机回环，扩展名白名单，单文件 ≤10MB，
  文本 ≤64KB 自动摘要，存 `DSH_HOME/attachments`）；输入区拖拽上传 + 附件片；创建任务随记录
  持久化，方案与子代理提示词携带附件名与摘录；删除编排自动清理附件文件。
- 快捷命令：`/new <任务>`、`/plan <反馈>`、`/memory`（记忆提示，2.5-C 实现）。
- @ 引用：输入 `@` 下拉引用想法库条目与当前项目文件（复用受限 `fs/list`）。
- 候选专家（可选，默认自由生成）：`GET/POST /api/dsh-workbench/agents/list|write`、
  `POST /api/dsh-workbench/agents/reset`；持久化到 `DSH_HOME/dsh-workbench-agents.json`
  （含 `mode: free|pool`，数组/去重校验）。默认"自由生成"——拆解时不参考固定名单，主代理
  按任务自动创建最适配的专家并自动分配模型；切到"参考候选池"后，池内角色/能力参与拆解匹配，
  方案可用 `agentRef` 标记命中，命中的子代理会注入池提示词，并以池 `provider/model` 作为
  未手动指定时的回退模型；"决策"Tab 提供模式开关、JSON 编辑器、重新载入/恢复默认/保存设置。
- 依赖可视化：方案 Tab 新增"执行顺序"条，按 `dependsOn` 拓扑分组展示并行/串行。
- 验证：`smoke-collab.mjs` 覆盖代理池（自由/参考模式、reset、agentRef 提示词与模型回退）/
  附件/日志/提示词注入/清理；smoke ×5、eslint、`node --check` 通过；CDP GUI 回归通过
  （四 Tab、日志、拖拽上传、@ 引用、/new、候选专家模式开关与保存/恢复默认、执行顺序条），
  截图 `collab-attach.png`、`collab-order.png`、`collab-agents-mode.png`；测试数据已清理。
- 部署：备份 `backups\workbench-p2.5-b-deploy-20260819-102254`，运行副本哈希一致，桌面端已重启
  （本轮 API 端口 58179，带 `--remote-debugging-port=9224`）。

2.5-C 待办：记忆快照（生成/持久化/记忆库 Tab/Token 恢复，需先确认 D16 范围）、项目工作区与
  上下文自动注入、成本护栏（全局并行上限 + Token 预算提示）。

2.5-C 记忆快照完成记录（2026-08-19，按 D16 推荐默认：仅存摘要/关键发现/决策/待办，不含原文与
代码全文）：

- Host：`DSH_HOME/dsh-workbench-memory.json` 持久化（上限 100 条）；`memory/list`、
  `memory/generate`（仅已结束任务，LLM 压缩，失败回退启发式提取）、`memory/remove`；
  任务创建支持 `memoryTokens`（≤5，无效 token 忽略），方案提示词注入记忆摘要作为跨会话上下文。
- 客户端：右侧面板新增"记忆"Tab（从当前任务生成/复制 Token/加载到输入/删除）；输入区记忆片；
  任务详情展示已携带的记忆。
- 验证：`smoke-memory.mjs` 通过（生成/拒绝未结束任务/无效 token 过滤/提示词注入/删除）；
  smoke ×6、eslint、`node --check` 通过；CDP GUI 回归通过（五 Tab、真实生成简历生成器快照、
  加载到输入并创建携带记忆的任务），截图 `collab-memory.png`；测试任务已清理、快照保留。
- 部署：备份 `backups\workbench-p2.5-c-memory-20260819-111959`，运行端已重启（本轮 API 端口
  51107，带 `--remote-debugging-port=9224`）。

2.5-C 项目上下文与成本护栏完成记录（2026-08-19）：

- 项目上下文自动注入：方案生成提示词自动携带项目文件结构（≤60 项）、技术栈线索（package.json
  等常见清单头部）与近期协作记录；仅限注册工作区路径（复用 `authorizeWorkspacePath`），越界/未
  注册自动跳过，不泄露。
- 成本护栏：概览 Tab 新增并行上限、预计 LLM 调用（1 + N）；子代理 ≥4 时提示 Token 消耗；
  引擎并行上限保持 ≤4。
- 验证：`smoke-collab` 新增项目上下文断言；smoke ×6、eslint、`node --check`、CDP 概览行检查通过；
  部署备份 `backups\workbench-p2.5c-context-20260819-113636`。
- **P2.5 全部完成**：会话化协作（模式/快速问答/附件/@引用/命令/日志/候选专家/记忆快照/项目上下文/
  成本护栏）已交付并部署，进入 P3 监控页。

### Phase 2.6：对话窗口多AI模式（P2.6，2026-08-19 计划）

> 需求：普通对话窗口（Agent 页）也加"单AI / 多AI"切换；切到多AI后对话窗口默认就是主代理，
> 发布任务时自动测试可用模型再分配。用户已选定推荐方案（接入方式 A、开关按会话记忆、
> 默认自动判断可强制编排、模型探测缓存 10 分钟失败回退、对话流折叠卡片 + 任务中心同数据、
> AI 协作页保留）。

#### 一、两项补判（原 P2.5 方案未做项，2026-08-19）

1. **A1 想法库抽屉 → 不建常驻抽屉，改为"@ 引用浮层"（D24）**：输入区点 @ 或图标弹出浮层，
   可浏览/搜索想法库条目与当前项目文件，点击即插入引用。理由：聊天场景抽屉占空间、操作重，
   浮层零成本唤起即点即插，且 AI 协作页已有 @ 引用可统一升级为同一套浮层。
2. **D1 项目工作区管理页 → 不建独立大页面，改为"项目上下文设置"轻量表单（D25）**：
   随项目绑定备注、注入目录范围、技术栈覆盖。理由：自动注入已覆盖 80%，缺的只是人工覆盖项；
   大页面成本高、维护负担重；与 P5 检索画像共用"项目配置"维度。

#### 二、功能设计（多AI模式下）

1. **对话窗口 = 主代理**：开关切到"多AI"后，当前会话即主代理，会话历史 + 项目上下文/记忆注入
   成为主代理上下文。
2. **发布任务自动测模型再分配**：发送消息 → Host 探测可用模型（目录 + 试跑 1 次极小请求验证，
   缓存 10 分钟）→ 主代理拆解子代理 → 从已验证模型列表分配（不可用自动回退主会话模型）→
   执行（复用看门狗）→ 主代理汇总。
3. **结果回到对话流**：折叠卡片（展开看子代理明细/模型/日志摘要），完整管理仍可去任务中心。
4. **简单问答**：多AI模式下默认自动判断——简单走快速回答（单个回答代理），复杂才全量编排；
   可设"始终编排"。

#### 三、实施步骤（固定循环）

1. 现状核验：确认原生 conversation 是否暴露编程发送接口 → 定 overlay 实现细节（方式 A）。
2. 对话输入条与开关：工作台接管输入区，单AI放行原生、多AI接管发送；开关按会话记忆。
3. @ 引用浮层 + 附件：统一引用面板（想法库+文件+搜索）+ 拖拽上传。
4. 多AI路由与进度卡：创建编排 → planning 卡 → 执行状态卡 → 主代理汇总卡。
5. 模型自动探测：Host `models/probe`（目录 + 试跑 + 缓存 + 回退）；卡片显示模型分配与探测结果。
6. 项目上下文设置（D25 轻量表单）。
7. 回归（CDP GUI 真实对话窗口）+ 部署 + 文档 + 提交。

#### 四、验收标准

- 普通对话窗口可见单/多AI开关且按会话记忆；单AI行为与原生一致。
- 多AI下发送复杂任务自动出方案卡、执行卡、汇总卡；简单问题直接回答。
- 每个子代理展示所用模型；探测失败的模型自动回退且原因可见。
- @ 浮层可浏览/搜索/插入想法库与文件；拖拽上传可用。
- 项目上下文设置可保存并影响提示词注入。
- 与任务中心同一份数据，双向可见；CDP GUI 回归通过后部署。

#### 五、实施与验证记录（2026-08-20，已部署并通过 GUI 验收）

- Client：Agent 页新增按会话记忆的“单 AI / 多 AI”切换；单 AI 保留原生输入，多 AI 接管输入并自动完成
  创建 → 模型实测 → 规划 → 启动 → 主代理汇总。复杂度默认自动选择快速回答/完整编排，可切换“始终编排”。
- 对话卡：展示阶段、进度、主/子代理状态、实测或继承模型、最终报告和验收风险；可精确跳转任务中心的
  同一条编排记录。待自动启动 ID 写入会话级 localStorage，规划期间切换会话后仍能恢复启动。
- 输入增强：新增想法库/项目顶层文件 @ 引用浮层，复用附件选择与拖放；引用以 `sourceRefs` 持久化并
  注入方案、子代理和主代理提示词。
- Host：新增 `POST /api/dsh-workbench/models/probe`，极小请求实测结果缓存 10 分钟，并发 3；目录较大时
  按 Provider 轮转抽取、单次最多实测 12 个，界面显示目录总数与跳过数；全部失败时继承主会话模型。
- 项目上下文：新增 `GET|POST /api/dsh-workbench/project-context`，保存备注、技术栈覆盖和项目内相对注入目录；
  存储采用串行原子替换、Windows rename 重试、1 MB/200 项上限，保存及每次读取均校验真实路径未越出项目。
- 生成侧溯源门：子代理和主代理被要求用 `[来源: 名称]` 标注引用结论；最终报告缺少标注时写入确定性
  验收风险，不把未溯源结果静默视为完成。
- 体验修正（2026-08-20 追加）：多 AI 发送后，用户消息与主代理最终报告（或失败原因）以消息卡直接写入
  对话流，观感与正常对话一致；底部浮层只保留“子代理运行情况”（主/子代理状态，正在工作的代理高亮
  “工作中”），不再展示报告/进度条/模型实测。浮层行为：任务进行时自动展开、可手动折叠（手动状态优先，
  新任务自动重置）、完成后自动折叠为标题条。模式栏移到头部下方右侧，避免遮挡右上角会话用量。
  消息按会话持久化，切换会话后仍可恢复。
- 源码验证：`node --check`（Client/Host）、`git diff --check`、ESLint（client/host 零报错）、8 项 smoke
  （collab/knowledge/memory/orchestration/style/watchdog/workflow/client-load）全部通过。
- 运行端：备份 `backups/workbench-p26-deploy-20260820`，Client/Host 复制后哈希与源码一致，桌面端以
  `--remote-debugging-port=9224` 重启。
- 真实 GUI 回归（`tools/gui-p26-regression.mjs`，截图 p26-agent-*.png 与 p26-task-center.png）：
  - 单 AI 原生输入保留；切多 AI 后工作台输入接管、原生输入隐藏；模式按会话记忆。
  - @ 浮层可打开并列出 14 条想法/文件；项目上下文对话框三字段与保存按钮正常。
  - 模型实测真实成功：目录 30 个、单次实测 12 个（全部可用）、跳过 18 个、10 分钟缓存生效。
  - 真实快速问答走通“创建 → 模型实测 → 规划 → 自动启动 → 子代理回答 → 主代理汇总”，最终报告 100% 完成；
    主代理 gpt-5.6-sol、子代理 deepseek-v4-flash，卡片与任务中心同一条记录；用户消息与主代理最终报告
    已作为对话消息出现在对话流（`.wb-chat-msg-user` / `.wb-chat-msg-assistant`）；浮层仅显示
    “子代理运行情况”，且工作时自动展开/可手动折叠/完成后自动折叠（CDP 实测三种状态均通过）；
    模式栏与用量控件无重叠（overlap=false、位于 header 下方）。
  - “在任务中心查看完整记录”精确高亮该编排（`wb-collab-row-active`），详情含方案、模型清单与实际使用模型。
- 本轮改动已提交（见 git 记录）；备份 `backups/workbench-p26-pre-20260820-000115` 保留。

### Phase 2.7：对话与项目配置体验（P2.7，2026-08-20）

> 用户确认三项方案（决策点均采用建议项），并先行核实行业实践：AGENTS.md 为 2025 年 OpenAI/Google/
> Anthropic/Cursor 联合推动的开放标准，跨工具兼容，因此项目规则采用 AGENTS.md。

#### 一、单 AI / 多 AI 切换移到左侧栏

- 从对话区右上角移到“临时想法”输入框下方，按会话记忆；对话区不再有模式栏，右上角用量控件不再被遮挡。
- 左侧栏收起时不提供额外入口（已确认）；会话切换时按钮自动跟随当前会话。

#### 二、多 AI 协作任务进入看板与复盘

- 创建编排时同步生成看板任务卡（带“AI 协作”标识，owner=agent）；删除编排时一并清理。
- 阶段自动同步：idea/planned→待执行；planning/running/refining/review→进行中；accepted→已完成；
  failed/cancelled→阻塞。
- 看板卡片点击跳转对应编排详情；拖拽/状态选择做安全回写：拖到“已完成”→验收通过（仅 review 阶段），
  拖到“阻塞”→确认后取消；其余状态只更新任务卡。
- 复盘统计（近 7 天完成 / 正在推进 / 完成率）天然包含协作任务。
- Host 在 `mutateTasks` 与 `queueOrchestrationPatch` 两处统一同步任务状态。

#### 三、项目配置独立成工具栏栏

- 右侧工具栏新增“项目配置”tab，三个区块：
  - 会话专属内容：查看/追加，存于应用数据目录 `sessions/<项目>/<会话>/dsh-workbench-session.md`
    （UTF-16 slug 与 harness-home 会话目录命名一致），**不写入项目文件夹**（2026-08-20 明确文案与行为）。
  - 项目规则：自动发现 AGENTS.md / CLAUDE.md / AGENT_RULES.md / .cursorrules / README /
    .cursor/rules/*.mdc；支持追加与“初始化 AGENTS.md”（决策点：AGENTS.md）。**明确会读写项目文件夹
    内的规则文件**，与“会话专属内容”在面板中分区注明，避免污染项目疑虑。
  - 项目备注：保留备注/技术栈/注入目录表单；新增“AI 自动精炼”（读 README/规则生成结构化备注，
    可一键填入）。
- 项目规则自动纳入多 AI 规划提示词注入（`projectContextSummary`）。
- 新增 Host 接口：`GET|POST /api/dsh-workbench/session-context`、`GET|POST /api/dsh-workbench/project-rules`、
  `POST /api/dsh-workbench/project-context/refine`；规则与会话文件均原子写入、限大小、防越界。

#### 四、验证

- ESLint 零报错；8 项 smoke 通过；`smoke-collab.mjs` 新增：编排任务卡生成与状态同步、规则初始化/追加、
  会话内容读写、规则注入规划提示词。
- 真实 GUI 回归（`tools/gui-p26-regression.mjs` 扩展）：左侧栏切换生效且旧模式栏移除；发送编排后
  看板出现带“AI 协作”标识的任务卡，复盘统计正常；项目配置面板三区块与 AI 精炼/初始化按钮齐全；
  失败路径（方案生成偶发 JSON 解析错误）正确显示在对话流、任务中心与看板。
- 部署备份：`backups/workbench-p27-20260820`。

#### 五、排后项收尾（2026-08-20）

- 窄窗口布局：`gui-p27-regression.mjs` 在 980×720 视口实测多 AI 浮层/输入区/左侧栏切换均无溢出
  （overflowX=0），通过。
- 规划 JSON 健壮性：`parseJsonObject` 增加截断 JSON 括号补全；方案生成失败自动重试一次；
  maxTokens 6000→8000。已用三种截断样例验证。
- “始终编排”复杂任务真实执行：成功生成 3 个子代理方案（deepseek-v4-pro / gpt-5.3-codex-spark /
  gpt-5.2-pro），watchdog 重试、任务卡状态同步、失败原因展示均正常；其中 worker1 重试后完成，
  worker2/worker3 重试后仍失败（子代理执行超时/会话报错）。**已知限制**：复杂子代理任务在
  300 秒内未完成或子代理会话偶发报错，worker 超时默认已由 5 分钟提高至 15 分钟；
  子代理会话稳定性（尤其 ai-gateway 模型）列入后续专项排查。

### Phase 3：监控页（P4）

五板块：

1. 账户总览：余额、累计费用、预计可用天数、费用趋势、是否峰值计价时段。
2. 用量统计：输入/缓存读/缓存写/输出 Token；按模型、按天；请求数；对话轮次；
   峰谷费用拆分。
3. 会话洞察：消耗最高会话、活跃会话、活跃天数、会话轨迹、时间线。
4. 实时面板：当前会话实时 Token/费用、运行状态、模型、专家。
5. 告警：余额/日预算/月预算阈值；页面内横幅 + 导航栏红点；不依赖系统通知。

数据源：`dsh-usage-stats`、原生 trajectory、Session 数据、本地费用配置。

验收标准：五板块可用；数据与本地统计源一致；峰谷计算正确；阈值触发页面内提醒。

完成记录（2026-08-19）：

- 数据源：`dsh-usage-stats`（summary/balance/sessions 同源回环接口）+ 会话 store；
  客户端每 10 秒刷新。
- 五板块全部实现：账户（余额/累计费用/预计可用天数/Token 总量/费用趋势）、用量（Token 分项/
  命中率/主要模型/按模型/按天，7-365 天区间）、会话（消耗最高前 8）、实时（当前会话用量 +
  协作运行状态）、告警（余额/日/区间阈值 → 页面横幅 + 导航红点，本机 localStorage）。
- 验证：CDP GUI 回归通过（五 Tab 渲染、真实数据：余额/累计费用/Token 分项/模型/按天），
  截图 `monitor-account.png`、`monitor-alerts.png`；eslint、`node --check`、client-load 通过。
- 说明：余额接口依赖 provider 是否暴露余额端点（当前 deepseek 显示 6.21 CNY，预计可用约 0.7 天）；
  峰谷计价拆分留待接入 pricing 数据后细化。

### Phase 4：工作流页（P5）

功能清单：

- 模板库与工作流卡片（名称、用途、使用的专家、使用的技能、一键运行、编辑、删除）。
- 运行状态、执行进度、成功/失败结果展示。
- 定时调度，基于原生 workflow 与 schedule 实现。

首批模板（决策点 D3，建议 4 个）：

- 日报/晨报汇总
- 会议纪要整理
- 调研写作流水线
- 表格数据清洗

验收标准：模板一键运行；状态与失败原因可见；模板可编辑；定时任务按计划触发。

完成记录（2026-08-19）：

- 模板库：默认 4 模板（D3 推荐：日报/晨报、会议纪要、调研写作、表格清洗）惰性种子化到任务库；
  支持新建/编辑/删除（`template_create|update|remove`）。
- 一键运行：`workflows/run` 复用任务库模板应用逻辑，生成分组任务并记录运行（done/failed、
  任务数、起止时间）；运行记录与调度持久化到 `DSH_HOME/dsh-workbench-workflows.json`。
- 定时调度：间隔分钟（1–10080）、启用/暂停、lastRunAt；Host 每 30 秒轮询，`unref()` 不阻塞
  退出；**限制：仅在桌面端运行期间触发**。
- 验证：`smoke-workflow.mjs`（默认模板/创建模板/运行生成任务/调度 CRUD/删除）通过；smoke ×7、
  eslint、`node --check` 通过；CDP GUI 回归通过（三 Tab、新建模板、运行、调度、运行记录），
  截图 `workflows-page.png`；测试数据已清理。

### Phase 5：知识库与蒸馏（P5，v3.1 方案已确认 2026-08-19）

> 用户最终确认：三层架构 / Obsidian 可视化增长 / 每项目检索画像 / 自生长机制 / MCP 层预留 /
> 评测集先行 / 向量路可插拔（初期不装模型，接口留好，后续可配本地 BGE 或付费 embedding）。
> 目标：本地自用、自生长、不污染上下文、最小 token 最大价值、防幻觉、多路可配置检索、
> 未来脱离工作台仍可被其他 agent/IDE 使用。

#### 一、三层架构

1. **资产层（Obsidian Vault）**
   - 目录：`01-Inbox`（AI 写入区）→ `02-Atomic`（人工审核区，核心资产）→ `03-MOCs`（地图索引）→
     `04-Projects`（按项目沉淀）→ `99-Templates`；默认 `DSH_HOME/knowledge/`，可在设置中改。
   - 每篇文档带 frontmatter：title / tags / related `[[...]]` / confidence / summary / source / created。
   - 工作台"知识库"页：增长仪表盘（文档总数、本周新增、双向链接数、待审核数、近 7 天趋势）+
     文档浏览 + 新建/编辑 + 一键在 Obsidian 中打开；图谱用 Obsidian 原生 Graph View。
2. **引擎层（检索与维护）**
   - 索引：frontmatter + 标题层级结构化分块、中文分词（CJK bigram + 英文单词）、增量索引（hash 判变）、
     指纹去重防重复入库。
   - 多路召回：BM25 关键词路 + 图谱路（标签/双向链接 2 跳）+ 向量路（可插拔，默认关闭）+
     HyDE 路（可选，LLM 改写问题）+ 项目上下文加权。
   - 融合重排：RRF 多路融合 → 可选 LLM 重排（DeepSeek）→ 证据块 token 预算（默认 Top5）→
     强制溯源（文件路径 + 置信度 + 检索分）。
   - 检索画像：每项目一套检索配置（启用路由/权重/TopK/token 预算/目录范围），可手动改。
   - 维护器：每周维护（合并重复、清理孤儿、断链修复、置信度降级）+ 反馈闭环
     （"没找到/答案不准"记入评测候选池）。
3. **接入层（工作台 + MCP 预留）**
   - 工作台：检索框、项目绑定画像、蒸馏入口、知识库页。
   - MCP 预留：稳定本地检索接口 + 独立查询脚本 `tools/knowledge-query.mjs` + 接口文档；
     将来其他 agent/IDE 直接调用，或包一层 MCP server。

#### 二、向量路（可插拔，D20）

- 接口：`provider: none | bge-local | openai | custom`，统一"文字 → 向量"；
- 初期默认 `none`：BM25 + 图谱 + DeepSeek 重排即可跑通，零成本；
- 后续不满意可配置：本地 BGE（免费离线，Python + ONNX runtime）或付费 embedding（OpenAI 等填 key）；
  一行配置切换，不改代码；是否启用由评测集实测决定（D21）。

#### 三、自生长四件套

写入即结构化（蒸馏自动带 frontmatter）→ 审核流转（Inbox → 人工确认 → Atomic）→
每周维护压缩（MOC 自动更新、去重、断链修复）→ 反馈闭环（使用中持续修正）。

#### 四、评测集先行（D21）

先建 20–50 条真实考题（问题 + 期望召回文档 + 答案要点），每次改引擎跑同一套题，
量化召回率（recall@5/10）、答案命中率、token 消耗；只保留变好的改动；向量路开关用它验证。
召回率测量纯本地；答案质量用 DeepSeek 可选。

#### 五、实施步骤

| 步骤 | 内容 | 状态 |
|---|---|---|
| 0 | vault 骨架 + 知识库页（仪表盘/浏览/一键 Obsidian）+ 索引 v2 | 已有骨架，v3.1 修订中 |
| 1 | 蒸馏器：会话/文档 → 结构化 Markdown → 01-Inbox（DeepSeek） | 待做 |
| 2 | 检索引擎：BM25 + 图谱 + 增量索引 + RRF 融合 + 溯源 + 可插拔向量接口 | 待做 |
| 3 | 工作台集成：检索框、项目检索画像、一键 Obsidian | 待做 |
| 4 | 评测集 + 回归脚本 + 首次基线跑分 | 待做 |
| 5 | 维护器 + 自生长闭环 + MOC 自动更新 | 待做 |
| 6 | MCP 层预留：检索接口 + 查询脚本 + 文档 | 待做 |
| 7 | 端到端验收 + 部署运行端 + 文档 + 提交 | 待做 |

验收标准：

1. 蒸馏全流程走通（源 → 01-Inbox 结构化 Markdown，DeepSeek 可用）。
2. 检索返回带溯源（路径/置信度/检索分）的结果，token 预算生效。
3. 每个项目可用不同检索画像；一键 Obsidian 打开可用（或给出明确 fallback）。
4. 评测集跑分可复现；每次引擎改动有对比记录。
5. 维护器可去重、断链报告、MOC 自动更新；反馈可入候选池。
6. MCP 预留接口文档 + CLI 脚本可用。
7. 端到端 smoke + GUI 回归通过，部署运行端并提交。

完成记录（2026-08-19，部署至运行端）：

- 资产层：vault 五目录（01-Inbox/02-Atomic/03-MOCs/04-Projects/99-Templates）+ Dashboard/README/.obsidian
  + 默认模板；frontmatter 解析修复（空字段不再跨行吞值）。
- 引擎层：索引 v2（增量 hash、统计、中文 bigram BM25）、图谱（双向链接/标签 2 跳）、RRF 融合、
  可选 LLM 重排/HyDE、证据块 token 预算与强制溯源；可插拔向量接口（none/bge-local/openai/custom），
  默认关闭，配置留空保持已保存 key。
- 蒸馏：AI 提炼 → 01-Inbox（DeepSeek 实测生效，失败兜底模板）。
- 维护器：去重/断链/孤儿/过期报告 + 03-MOCs/Index.md 自动更新；反馈入评测候选池。
- 评测集：eval 增删/运行 recall@k/token/耗时；CLI `run-knowledge-eval.mjs`。
- 接入层：知识库页六 Tab（增长仪表盘/浏览/检索/蒸馏/维护/评测/向量设置）、预览编辑、一键 Obsidian、
  复制路径；MCP 预留 CLI `knowledge-query.mjs` + 本地 BGE 桥 `knowledge_embed.py`。
- 验证：`smoke-knowledge`（vault/写读删/检索/画像/蒸馏/维护/评测/向量 mock provider 端到端）+
  8 个 smoke 全过、eslint 0、tsc ×4；CDP GUI 回归通过（仪表盘/检索溯源/真实 AI 蒸馏），
  截图 knowledge-dash.png / knowledge-search.png / knowledge-distill.png。
- 部署：备份 `backups\workbench-p5-knowledge-deploy-20260819-154456`，运行端已替换并重启
  （本轮 API 端口 62713，带 --remote-debugging-port=9224）。

第二迭代（2026-08-19，用户实机反馈后）：让本地知识库真正可用

- 体验修复：新建条目表单全局可见（不再只在浏览 Tab 下）、改为结构化表单（标题/类型/标签/置信度/
  关联/摘要/正文自动生成 frontmatter）；检索空结果给出目录范围提示。
- 检索增强：标签（含相关标题）纳入索引；检索前自动检测 vault 变更（mtime 指纹），Obsidian/
  直接改文件无需手动同步即可搜到。
- 资产总览：新增"总览"Tab——知识库技能池（type: skill 或标签 技能）、项目（type: project 或
  04-Projects + 注册工作区）、工作流（type: workflow 或标签 工作流 + 工作流页模板库）、
  专家技能（preset.yml 的 skills: 列表，与知识库技能池统一）；条目 frontmatter 新增 type。
- 验证：smoke 新增标签检索/直接写入自动重建/总览分组断言；CDP GUI 回归新增新建条目全流程与
  总览 Tab 断言，全部通过；部署备份 `backups\workbench-p5-overview-deploy-20260819-161129`。
- Obsidian：已安装 1.13.7（静默安装到 `%LOCALAPPDATA%\Programs\Obsidian`），vault 已注册并打开
  （`%APPDATA%\obsidian\obsidian.json` 写入 knowledge vault）；Dataview 0.5.70 + Excalidraw 2.25.3
  已放入 `.obsidian\plugins` 并启用（首次打开 vault 需点一次"信任作者并启用插件"）。

第三迭代（2026-08-19，评测 + 自生长优化）：

- 造知识 + 评测集：`tools/seed-knowledge-workbench.mjs` 写入 18 条工作台真实知识（任务面板/风格页/
  多AI协作/看门狗/记忆/候选专家/监控/工作流/知识库架构/检索/蒸馏/部署/文件夹修复/简历生成器/
  FastAPI/BGE/维护器/评测集）+ 25 条评测题。
- 基线跑分：recall@5 = 0.96（24/25），唯一 miss 是 04-Projects 条目——默认检索范围未含 projects。
- 调优：默认检索范围加入 04-Projects（模板仍默认排除），重跑 recall@5 = **1.0**（25/25），
  平均 268 token / 3ms。
- 自生长优化：检索无结果自动记入评测候选池（去重）；评测历史存档（最近 10 次，可对比调优）；
  维护器每 24h 自动运行（unref 不阻塞）+ 启动 15s 后自动维护一轮（MOC/去重/断链）。
- 验证：smoke 新增历史存档与空检索自动候选断言；全套 8 smoke + eslint 通过；已部署
  （备份 `backups\workbench-p5-selfgrow-deploy-20260819-163600`）。

第四迭代（v4 质量门与召回路由，2026-08-19 方案已确认，D30–D35 全部按推荐项执行）：

> 目标：知识库从"能检索"升级为"可信、可验证、可自生长"——乱入库、乱置信、乱召回三个问题
> 在本迭代收口。核心是四件事：源材料与成品隔离（raw/）、入库六步质量门、置信度有依据地推导、
> 按查询类型自动选择召回路由（向量路必开）。

#### 一、存储分层与元数据升级

1. 新增 `00-Raw/`（源材料层，污染隔离，默认不进检索）与 `05-Archive/`（遗忘归档层，D34）。
2. 条目 frontmatter 升级：`status（draft/review/published/deprecated）`、`claimType（fact/hypothesis）`、
   `assumptions（假设清单）`、`source（必填）`、`verifiedBy / verifiedAt`、`staleness（STABLE/CHECK/VOLATILE）`；
   旧条目按目录推导默认状态（inbox→review，atomic/projects/mocs→published，raw/templates→draft）。
3. 默认检索范围 = 已发布条目（published）+ 02-Atomic 为主、04-Projects/03-MOCs 可召回；
   raw/、archive/ 与未发布草稿默认不参与检索（可通过检索画像显式开启）。

#### 二、入库六步质量门（D31）

捕获（00-Raw 存源材料）→ 预检（查重/同主题冲突/完整性）→ 提炼（蒸馏，结论必须带 source）→
验证门（默认人工确认；`reviewMode=auto` 时低风险条目自动发布）→ 发布（status=published +
verifiedBy/At，可一键移入 02-Atomic）→ 入索引（文本 + 向量 + 图谱同步更新）。

- 预检返回：精确重名、高相似候选、同主题冲突提示、缺失字段警告；可强制写入但必须看到警告。
- 低风险自动发布条件（D31 推荐）：source 非空、无重名/高相似块、蒸馏置信度不为低。

#### 三、置信度推导（D33）

置信度 = 来源可信度 × 验证状态 × 一致性 × 新鲜度 × 使用历史（命中强化、长期未用衰减）。
界面与检索结果同时展示"推导置信度 + 依据（各因子得分与原因）"，不再只有孤零零一个标签。

#### 四、召回路由（D30）

- 查询自动分类：精确术语 → BM25 + 图谱；语义模糊 → 向量；关系类 → 图谱加权；综合 → 混合 + HyDE + 重排。
- 画像新增 `mode: auto | bm25-graph | graph | vector | hybrid`，默认 auto；返回结果带 routing 说明。
- 向量路必开：默认 Provider 改为本地 `bge-local`（bge-small-zh-v1.5，免费离线），
  默认画像 `routes.vector=true`；接口保持可插拔，失败时优雅降级并在结果中标注。
- 自纠错门（D35）：本迭代做检索侧——低置信/低分结果加 caution 提示，空检索自动入评测候选池；
  生成侧（回答引用知识时的强制溯源与自纠）并入 P2.6。

#### 五、使用与遗忘（D34）

- 检索结果与 CLI 强制带：文件路径 + 推导置信度 + 依据 + 检索分 + 原始来源 + 状态。
- 遗忘默认只提示（维护器列出 180 天未更新条目并建议归档）；自动归档需在质量门显式授权开启。

#### 实施清单（本迭代）

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1 | 计划更新（本文档 + D30–D35） | ✅ 完成 |
| 2 | Host：存储分层 + frontmatter/索引元数据升级 + 置信度推导 | ✅ 完成 |
| 3 | Host：六步质量门（precheck/publish/archive/quality/raw 接口）+ 自动发布 | ✅ 完成 |
| 4 | Host：召回路由分类 + 向量路默认开启 + 使用历史跟踪 + 自纠错门检索侧 | ✅ 完成 |
| 5 | 本地 BGE：pip 安装 onnxruntime/tokenizers/numpy + 模型下载 + 向量重建 | ✅ 完成 |
| 6 | Client：表单/质量门页/检索结果/蒸馏/维护页更新 | ✅ 完成 |
| 7 | CLI + smoke 更新；全套回归（smoke/eslint/tsc/评测集跑分） | ✅ 完成 |
| 8 | 部署运行端 + 真实数据评测 + GUI 回归 + 文档 + 提交 | ✅ 完成 |

完成记录（2026-08-19，已部署）：

- 存储分层：`00-Raw/` 源材料层（捕获接口 + 默认不进检索）、`05-Archive/` 归档层已建；
  frontmatter 升级 status/claimType/assumptions/verifiedBy/verifiedAt/staleness，旧条目按目录推导状态。
- 质量门：`knowledge/raw|precheck|publish|archive|quality` 五个新接口；写入与蒸馏自动附带预检
  （重名/高相似/缺 source/正文过短）；默认人工确认，`reviewMode=auto` 时低风险蒸馏自动发布到 02-Atomic。
- 置信度：来源可信度 × 验证状态 × 一致性 × 新鲜度 × 使用历史（检索命中强化、90/180 天未用衰减），
  浏览卡片与检索结果均显示"推导置信度 + 依据"，CLI 同步输出。
- 召回路由：查询自动分类（bm25-graph / graph / vector / hybrid），画像新增 `mode`；
  本地 BGE（bge-small-zh-v1.5，ONNX，512 维）默认启用并重建全部向量；语义查询实测走向量路。
- 自纠错（检索侧）：空检索自动入评测候选池；向量路异常/最高分低置信给出 caution 提示；生成侧并入 P2.6。
- 遗忘：默认只提示（180 天未更新列出建议），质量门可显式授权自动归档；维护器新增 conflicts 写入与
  archived 报告。
- 验证：8 个 smoke 全过、eslint 0、tsc ×4 全过；真实评测集 recall@5 = **1.0**（25/25）、
  平均 242 token / 261ms；CDP GUI 回归通过（新表单字段/质量门 Tab/检索路由/置信度依据/蒸馏/总览）。
- BGE 修复两处：stdin 显式 UTF-8 解码（Windows GBK 乱码）、ONNX 输入补 token_type_ids、
  自动下载仓库改为带 ONNX 的 Xenova 版。
- 部署：备份 `backups\workbench-p5-v4-deploy-20260819-170834`；运行端 lib + tools/knowledge_embed.py
  已替换并重启（本轮 API 端口 53875，CDP 9224）。

补充修复（2026-08-19，已部署）：

- 知识库页「在 Obsidian 中打开」失效：根因是 Electron 渲染层静默拦截 obsidian:// 外部协议跳转
  （window.location.href 无效）。改为新增 Host 路由 `/api/dsh-workbench/open/external`
  （Electron shell.openExternal，仅放行 obsidian:// 与 http(s)://），客户端按钮改走该接口；
  实测点击后成功唤起 Obsidian。
- 双存储同步补强：此前直接编辑 vault 文件只会自动刷新文本索引，向量索引需手动重建。
  现在写入口写入后、以及检索发现外部改动（Obsidian 直接编辑）时，都会增量更新受影响条目的向量，
  无需手动「重建向量索引」；实测编辑 02-Atomic 文件后文本检索即时命中且向量库 updatedAt 自动更新。
- 回归脚本兼容折叠导航（按 title 找知识库按钮）；smoke 增加 open/external 校验。
- 部署：备份 `backups\workbench-obsidian-open-fix-20260819-171514`；运行端已替换并重启
  （本轮 API 端口 60347，CDP 9224）。

第五迭代（v5 知识库总览分类中心，2026-08-19 已部署）：

- 总览页重构为分类中心：顶部四个板块 Tab（技能 / 项目 / 知识点 / 工作流，带数量），
  点击切换分类逐个细看；移除「专家专属技能」板块，技能池统一展示（专家间重复技能不再单列）。
- 分类互斥不重复：技能（type:skill 或标签 技能）→ 项目（type:project 或 04-Projects）→
  工作流 → 其余为知识点（排除 raw/archive/templates/mocs）。
- 条目卡片可展开详情：显示标签、用途/摘要、来源、关联条目、置信度依据、正文预览、路径，
  并提供「查看/编辑」「在 Obsidian 打开该文件」「复制路径」；技能条目摘要即"这个技能是干什么的"。
- 项目板块含工作区项目（一键打开工作区会话，按路径匹配 workspaceId）；工作流板块含模板库
  （一键跳到工作流页，onNavigate 贯通）。
- Host overview 返回互斥分组 + 详情字段（name/related/source/status/staleness/置信度依据等）。
- 验证：8 个 smoke（含 overview 分组/互斥断言）、eslint、tsc ×4 全过；CDP GUI 回归通过
  （分类 Tab 切换、技能卡片展开详情、四板块条目展示）；真实数据分组：技能 3 / 项目 1 /
  知识点 12 / 工作流 2 / 工作区 3 / 模板 4。
- 部署：备份 `backups\workbench-kb-overview-v5-20260819-172310`；运行端已替换并重启
  （本轮 API 端口 49796，CDP 9224）。

第六迭代（v6 项目经验 + 半自动提炼，2026-08-19 已部署；含 B1 修复）：

- 新增条目类型 `experience`（项目经验）：frontmatter 增加 context（情境）/ result（验证结果）/
  reusable（可复用结论）；新建表单按类型显示对应字段；99-Templates 新增「项目经验模板.md」。
- 蒸馏自动判别（D43）：AI 按内容来源自动选 note（概念问答/学习）vs experience（项目决策/踩坑/复盘），
  并在 frontmatter 写入对应字段；人工新建同样可选类型。
- 存放（D40）：项目经验默认沉淀在 02-Atomic 核心资产，用 project 字段与项目/工作区关联；
  04-Projects 收敛为项目档案定位。
- 总览新增「项目经验」分类（D42，中文"项目经验"）：互斥分组（技能 → 项目经验 → 项目 → 工作流 →
  知识点），模板/源材料/归档不混入任何分组；卡片展开显示情境/验证结果/可复用结论。
- 半自动提炼（D41）：维护器扫描已发布且被使用/新近的项目经验，自动生成「经验 → 知识点」草稿
  （LLM 提炼，失败兜底为结构模板）；维护页「确认提炼」落盘 knowledge 条目到 02-Atomic（source=
  项目经验提炼、precheck 排除源经验、源经验自动加回链），「忽略」跳过；失效建议自动清理。
- 全自动验证预案（未启用，已留接口）：门槛 = precheck 无阻塞 + 推导置信度≥中 + 源经验命中≥3 次；
  每次自动落盘写审计字段（appliedAt/来源），可进评测集回溯，出问题可一键回退（删除新条目）。
- B1 修复（见 3.5 Bug 队列）：新会话不再自动带出其他会话/项目的编排子代理，空态提示明确。
- 验证：smoke（experience 分组互斥 / 模板不泄漏 / 半自动提炼全流程）、eslint、tsc ×4 全过；
  CDP GUI 回归通过（项目经验 Tab / 经验卡片展开 / 全部分类切换）；真实数据 experiences=0（尚无真实经验条目）。
- 部署：备份 `backups\workbench-b1-plus-kb-v6-20260819-173452` 与
  `backups\workbench-kb-v6-fix-20260819-173601`；运行端已替换并重启（本轮 API 端口 65474，CDP 9224）。

验收标准：

1. 新条目/蒸馏产物默认不污染检索（draft/review 不进默认检索；发布后才可召回）。
2. 预检能指出重名/相似/缺 source；发布后置信度带依据展示。
3. 查询分类正确切换路由；向量路在本地 BGE 上跑通且纳入混合检索。
4. 检索结果带路径/置信度/依据/检索分/来源；CLI 同样输出。
5. 维护器只提示遗忘项，不擅自归档；授权后自动归档可用。
6. 评测集跑分不劣于 v3.1 基线（recall@5 = 1.0），并在向量开启后复核。

### Phase 6：打磨与文档（P7）

任务：

- 全局视觉统一；空状态/加载状态/错误状态统一；按钮与表单样式统一。
- 响应式与窗口尺寸适配；性能优化。
- 插件源码可维护性整理（拆分与注释）。
- 安全边界复查（文件接口、Host 路由、凭据）。
- 回滚流程演练（禁用/恢复工作台）。
- 文档：README、安装/升级/卸载说明、用户手册、故障排查说明。

验收标准：六页操作顺畅；无白屏崩溃；插件可安全禁用恢复；全套文档与功能一致。

### Phase 7：发布链路完善（可与 P2–P6 并行）

#### 7.1 数字签名（决策点 D7）

- 需要 Windows 代码签名证书（`CSC_LINK` / `WIN_CSC_LINK` + 密码）。
- 签名命令独立证书 preflight；签名后校验安装器、卸载器、主程序签名。
- SmartScreen 信誉是后续运营项。

#### 7.2 覆盖安装怪癖

- 调查并修复 NSIS "application cannot be closed"（无进程运行时仍提示）。
- 目标：2.0.2 起覆盖安装不再需要先卸载。

#### 7.3 卸载残留

- 清理 uninstaller 遗留的 sourcemap 文件（`@opentelemetry/resources/...`），
  或确认为无害并记录。

#### 7.4 基线文档持续同步

- 每次发布更新 `version-baseline.md`（版本、哈希、更新策略）。

#### 7.5 发布 SOP 固化

每版发布流程：

1. 同步版本号：`desktop-source/package.json` 与 `dsh-plugin-desktop/package.json`
   （门禁强制一致），README 版本引用。
2. `node scripts/package-win.ts`（内置门禁 + 安装器校验）。
3. `gh release create vX.Y.Z <Setup.exe> --repo wang291010/deepseek-harness-desktop`。
4. `git push`。
5. 已装应用自动更新或手动"Check for Updates"验证。

#### 7.6 跨平台（决策点 D9）

- 声明支持 mac/Linux 前：在 macOS 上跑 `dist:mac` + 公证（`release-preflight.ts` 已实现）；
- 清理 Windows 上 23 个 POSIX/mac 用例失败（环境差异，记录在案）。

#### 7.7 数据迁移（决策点 D6）

- `docs/desktop-data-migration-plan.md` 1–10 步已设计未执行。
- 默认建议：不自动迁移；提供只读查看与按需导入工具。

#### 7.8 便携版（决策点 D8）

- 如需要免安装单文件 exe：增加 portable 打包目标，并补自动更新适配
  （当前自动更新只适配安装版）。

完成记录（2026-08-19，8dbf932，另一会话）：

- 自动更新托盘开关：`desktop-updates` 插件注册持久化设置 `dsh-desktop-updates.enabled`
  （默认沿用合成补丁策略），托盘新增 "Automatic Updates: On/Off" 项；关闭后立即停止后台
  检查与弹窗，保留手动 "Check for Updates"；重新打开会调度下一次后台检查；偏好写入设置文档
  并跨重启保留。
- 验证：`updates.spec.ts` 21/22 通过（1 个失败为 Windows 下 POSIX 权限位断言的既有环境差异），
  tsc ×4 通过；已推送 origin/main。
- 运行端：新构建 lib 已部署到运行端 `resources\app.asar.unpacked\lib`（备份
  `backups\desktop-plugin-lib-before-updates-toggle-20260819-110438`），重启验证通过；
  与工作台插件（plugins/workbench）相互独立。

## 3. 决策清单（需要用户拍板）

| 编号 | 决策点 | 建议默认值 | 影响 |
|---|---|---|---|
| D1 | 模型重写任务 vs 手动任务的覆盖策略 | 保留模型重写，但增加"手动锁定"；如不能接受覆盖则建独立任务数据层 | 任务中心数据架构 |
| D2 | 文件视图允许目录范围 | 默认仅当前项目目录，可配置扩展 | 文件接口安全模型 |
| D3 | 工作流首批模板 | 日报/晨报、会议纪要、调研写作、表格数据清洗 4 个 | P5 开发范围 |
| D4 | 知识库形态与入库方式 | 已确认 v3.1：Obsidian 兼容 Markdown Vault（01-Inbox→02-Atomic→03-MOCs→04-Projects），蒸馏半自动（AI 生成 → 01-Inbox → 人工确认 → 02-Atomic） | P5 开发范围 |
| D5 | 知识库检索注入方式 | 已确认 v3.1：检索框手动触发 + 每项目检索画像；后续评估自动注入 | P5 交互设计 |
| D6 | 旧 DSH 数据迁移 | 不自动迁移，提供只读查看 + 按需导入 | 数据安全 |
| D7 | 数字签名 | 等证书；无证书前保持未签名发布 | 发布前置 |
| D8 | 便携版 | 暂不做；需要时补 portable + 自动更新适配 | 分享方式 |
| D9 | mac/Linux 支持 | 暂不声明支持；记录为未来项 | 发布范围 |
| D10 | 版本节奏 | 功能回归完成后发 2.1.0；期间 bug 修复发 2.0.x | 发布计划 |
| D11 | 选文件夹的实现方式 | 已升级（2026-08-19）：优先 Host 原生对话框（`fs/pick-folder`，Electron `dialog.showOpenDialog`），HTML 文件夹输入兜底 | P1A 项目创建 |
| D12 | 方案生成是否异步化 | 异步：先落盘 `planning` 再后台生成，请求立即返回，客户端轮询显示进度 | P1A 规划进度 |
| D13 | 执行进度刷新机制 | MVP 保持 1.5–2s 轮询 + 状态卡；后续如需要实时性再升级 SSE/WebSocket | P1A 进度可视化 |
| D14 | 多AI协作默认模式与自动判断 | 默认普通模式，用户手动切换多AI（推荐）；自动复杂度判断默认关闭，后续可开 | P2.5 交互与成本 |
| D15 | 复杂度判断实现方式 | 启发式规则 + 可选 LLM JSON 双轨，阈值可配置（推荐）；纯规则或纯 LLM 为备选 | P2.5 任务判定 |
| D16 | 记忆快照范围与存储 | 仅保存摘要/关键发现/决策/待办，不含对话原文与代码全文（推荐）；Token 即短 id；是否含代码 diff 待定 | P2.5 数据与隐私 |
| D17 | 文件上传限制 | 仅注册工作区内文件，单文件 ≤10MB、文本大文件自动摘要（推荐） | P2.5 安全 |
| D18 | 并行与成本上限 | 全局并行子代理上限默认 2–3；增加任务/每日 Token 预算提示 | P2.5 成本 |
| D19 | 旧界面去留 | 任务中心保留为历史/管理视图，主会话为日常入口（推荐） | P2.5 迁移 |
| D20 | 向量路实现 | 可插拔接口（none/bge-local/openai/custom），初期默认 none（BM25+图谱+LLM 重排），后续按需配置；本地 BGE 用 Python + ONNX runtime，付费模型填 key | P5 引擎 |
| D21 | 评测集 | 先行建立 20–50 条真实考题，引擎改动必须回归跑分，只保留变好的改动；向量路开关用评测实测 | P5 质量 |
| D22 | 维护与 MOC | 每周维护（去重/断链/置信度降级）+ MOC 自动更新 + 反馈闭环入候选池 | P5 自生长 |
| D23 | 对话窗口多AI模式 | 已确认（2026-08-19）：接入方式 A（工作台接管输入条）；开关按会话记忆；消息策略默认自动判断可强制编排；模型探测=目录+试跑1次+缓存10分钟+失败回退；结果=对话流折叠卡片+任务中心同数据；AI协作页保留 | P2.6 范围 |
| D24 | 想法库引用形态 | 已确认（2026-08-19）：不做常驻抽屉，改为 @ 引用浮层（浏览/搜索/点击插入），与 P2.6 输入条统一落地 | P2.6 范围 |
| D25 | 项目上下文设置 | 已确认（2026-08-19）：不做独立管理页，做随项目绑定的轻量表单（备注/注入目录/技术栈覆盖） | P2.6 范围 |
| D30 | 向量路 Provider | 已确认（2026-08-19 v4）：默认本地 BGE（bge-small-zh-v1.5，免费离线），可插拔换 openai/custom；默认画像 vector=true | P5 v4 引擎 |
| D31 | 审核模式 | 已确认（2026-08-19 v4）：默认人工确认；`reviewMode=auto` 时低风险条目（source 非空、无重名/高相似、置信度非低）自动发布 | P5 v4 质量门 |
| D32 | raw/ 源材料层 | 已确认（2026-08-19 v4）：新建 00-Raw/，先小规模（捕获入口 + 默认不进检索） | P5 v4 存储 |
| D33 | 置信度依据展示 | 已确认（2026-08-19 v4）：界面与检索结果显示推导置信度 + 依据（来源/验证/一致性/新鲜度/使用） | P5 v4 置信度 |
| D34 | 遗忘策略 | 已确认（2026-08-19 v4）：默认只提示不归档；自动归档需在质量门显式授权 | P5 v4 维护 |
| D35 | 自纠错门范围 | 已确认（2026-08-19 v4）：本迭代做检索侧（低置信/低分 caution + 空检索候选）；生成侧并入 P2.6 | P5 v4 + P2.6 |
| D40 | 项目经验存放位置 | 已确认（2026-08-19 v6）：项目经验默认沉淀在 02-Atomic（核心资产）+ project 字段关联；04-Projects 收敛为"项目档案" | P5 v6 |
| D41 | 经验 → 知识提炼 | 已确认（2026-08-19 v6）：**半自动**——系统生成提炼草稿 + 人工确认落盘；全自动作为可选模式，需过 precheck + 置信度门槛 + 源经验命中次数门槛并记录审计日志 | P5 v6 |
| D42 | 类型命名 | 已确认（2026-08-19 v6）：类型值 `experience`，中文显示"项目经验" | P5 v6 |
| D43 | 蒸馏自动判别 | 已确认（2026-08-19 v6）：AI 按内容来源自动判别 note（概念问答/学习）vs experience（项目决策/踩坑/复盘） | P5 v6 |

## 3.5 待修复 Bug 队列（2026-08-19 登记）

| 编号 | Bug 描述 | 根因（已定位） | 影响 | 优先级 / 排序 |
|---|---|---|---|---|
| B1 | 新开对话窗口（未用子代理）顶部却显示旧项目（简历生成器）的子代理 | `WorkbenchOrchestration` 的 `selected` 在无选中项时回退 `items[0]`，而 `scope=all` 会返回**全部项目**的编排记录并按 updatedAt 倒序；新会话没有自己的编排时，误把最近一次编排（简历生成器，6 个子代理）当作当前内容展示 | 新对话被旧任务污染显示，用户误以为当前对话在跑子代理；多项目时还会串到其他项目 | ✅ 已修复（2026-08-19）：仅当当前会话拥有编排 / 显式切到全部或全局 / 指定 initialId 时才自动选中；否则空态提示。随 v6 一起部署 |

修复方案（P2.6 前置，小步可验收）：

1. 无 `initialId` 且当前会话没有任何编排记录时，不再自动回退全局 `items[0]`，改为空态提示（"此会话还没有 AI 协作任务"）。
2. 编排列表默认按当前会话绑定过滤（`sourceSessionId` 匹配，其次 projectPath 匹配）；只有手动切到"全部/全局"才展示其他项目的编排。
3. 会话切换时重置选中项，避免上一个会话的编排残留。
4. 回归：新建空会话 → 协作视图为空态；旧项目会话 → 仍能正常看到该项目历史编排。

## 4. 执行方式与后续流程

1. 用户确认本计划（可逐条修改）。
2. 按 Phase 0 → Phase 1 → … → Phase 6 顺序执行；Phase 7 文档类事项可与功能开发并行。
3. 每个板块开工前，先输出该板块的"现状核验 + 方案 + 决策点"，用户确认后实施。
4. 每个板块完成后更新本文档状态与交接文档，再进入下一板块。
5. 计划变更必须更新本文档并经确认，否则按原计划执行。

## 5. 未完成事项总清单与重规划（2026-08-20）

> 本文档为唯一执行依据。以下按优先级重排未完成事项；已完成的 P2.6/P2.7 不再列入。
> 状态图例：🟡 待用户实测 / 🔵 待实施 / ⚪ 待拍板 / 🧊 暂缓。

### 队列一：验证与收口（已有代码，缺真机确认）— 建议最先做

| # | 事项 | 内容 | 状态 | 前置 | 建议排期 |
|---|---|---|---|---|---|
| V1 | P1A 任务面板重构真机复测 | 7 个用户反馈修复（快速创建/转交/规划异步/进度可视化/反馈分离/选文件夹/错误复制）+ 运行中交互回归 + 中断任务继续执行 | 🟡 已实现并部署（71e3479），待真机复测 | 无 | 下次用户实测 |
| V2 | P1B 其余运行端回归 | 任务中心存量（1B.1）、文件视图+安全收口（1B.2）、外壳与导航（1B.3）、专家页（1B.4） | 🟡 代码项完成（5fc2345），GUI 待实测 | V1 | 随 V1 一起 |
| V3 | 双客户端同会话风险提示 | P1A 待办，一并收口 | 🔵 未完成 | V1 | V1 后 |

### 队列二：稳定性专项（本轮实测发现）— 建议优先投入

| # | 事项 | 内容 | 状态 | 前置 | 建议排期 |
|---|---|---|---|---|---|
| S1 | 多代理执行稳定性排查 | 子代理会话偶发报错（尤其 ai-gateway 模型）、失败原因可诊断/可复制、并发资源与重试策略复核；已先行：规划 JSON 截断自修复+重试、worker 超时默认 5→15 分钟 | 🔵 专项启动 | 无 | 最近一轮 |
| S2 | 知识库评测集补题 | D21：从 25 题补至 50 题，引擎改动必须回归跑分 | 🔵 未完成 | 无 | 与 S1 并行 |
| S3 | 知识库使用期积累 | 用户实际使用 v6 后反馈闭环（体验问题优先修） | 🟡 持续 | 用户使用 | 持续 |

### 队列三：功能增强候选（按需启用）

| # | 事项 | 内容 | 状态 | 前置 | 建议排期 |
|---|---|---|---|---|---|
| F1 | 会话专属内容自动注入 | 项目配置栏“会话专属内容”目前只存储/查看/追加，建议纳入多 AI 规划提示词（项目规则已注入） | 🔵 未实施 | 无 | 队列二后 |
| F2 | 模型探测“全部实测” | 当前单次最多实测 12 个并显示跳过数；增加“全部实测”按钮 | 🔵 未实施 | 无 | 按需 |
| F3 | 对话风格按会话保存 | P2 遗留：当前全局默认 MVP，后续按会话保存 | 🔵 未实施 | 无 | 按需 |
| F4 | 执行进度实时化 | D13：1.5–2s 轮询 → 可选 SSE/WebSocket | 🔵 未实施 | 无 | 按需 |
| F5 | 任务手动锁定 | D1：模型重写 vs 手动任务的覆盖策略拍板后实施“手动锁定”或独立任务层 | ⚪ 待拍板 | D1 | 拍板后 |

### 队列四：P6 打磨与文档

| # | 事项 | 内容 | 状态 | 前置 | 建议排期 |
|---|---|---|---|---|---|
| M1 | 视觉与状态统一 | 全局视觉统一；空/加载/错误状态统一；按钮与表单样式统一 | 🔵 待执行 | 队列一二 | 功能稳定后 |
| M2 | 响应式与性能 | 窗口尺寸适配（窄窗口已测 980px）；性能优化 | 🔵 待执行 | 同上 | 同上 |
| M3 | 源码可维护性 | 拆分与注释整理 | 🔵 待执行 | 同上 | 同上 |
| M4 | 安全边界复查 | 文件接口、Host 路由、凭据复查；回滚流程演练 | 🔵 待执行 | 同上 | 同上 |
| M5 | 全套文档 | README、安装/升级/卸载说明、用户手册、故障排查 | 🔵 待执行 | 功能冻结 | M4 后 |

### 队列五：P7 发布链路

| # | 事项 | 内容 | 状态 | 前置 | 建议排期 |
|---|---|---|---|---|---|
| R1 | 版本节奏拍板 | D10：功能回归完成后发 2.1.0，期间 bug 修复发 2.0.x | ⚪ 待拍板 | 无 | 下次发布前 |
| R2 | 覆盖安装怪癖 | NSIS “application cannot be closed” 修复，2.0.2 起免卸载覆盖安装 | 🔵 待执行 | 无 | 发布前 |
| R3 | 卸载残留 | 清理/确认 uninstaller 遗留 sourcemap | 🔵 待执行 | 无 | 发布前 |
| R4 | 发布 SOP 固化 | 版本同步门禁、打包、release、自动更新验证 | 🔵 部分就绪 | R1 | 发布时 |
| R5 | 朋友安装包大更新 | 工作台安装包打包 + 内置 Node 运行时问题解决 | 🔵 待执行（用户重点关注） | P6 稳定 | 攒够进度后 |
| R6 | 基线文档同步 | 每次发布更新 version-baseline.md | 🔵 持续 | 发布时 | 持续 |
| R7 | 签名/跨平台/便携版/迁移 | D7/D8/D9/D6：等证书；mac/Linux 暂不声明；便携版暂不做；旧数据不自动迁移 | 🧊 暂缓 | 决策默认值已定 | 未来 |

### 待拍板决策（沿用默认值即可推进，拍板后可调整）

| 编号 | 决策点 | 当前默认 |
|---|---|---|
| D1 | 模型重写 vs 手动任务 | 保留模型重写 + “手动锁定”（如不接受覆盖则独立任务层） |
| D2 | 文件视图允许目录范围 | 默认仅当前项目目录，可配置扩展 |
| D10 | 版本节奏 | 功能完成发 2.1.0；bug 修复发 2.0.x |
| D16 | 记忆快照是否含代码 diff | 不含（仅摘要/发现/决策/待办） |
| D18 | 并行上限与成本预算 | 全局并行 2–3；增加任务/每日 Token 预算提示 |

### 执行进展（2026-08-20）

**队列一 V1/V2 代跑回归（`tools/gui-p1-regression.mjs`，通过）**：

- 会话面板：更多菜单 / 新建会话 / 创建项目 / 模式切换齐全；创建项目“浏览…”选文件夹可用。
- 任务中心：聚焦 / 想法库 / 任务 / AI 协作 / 复盘 五个主视图；看板 / 列表 / 时间线 / 模板四个子视图
  均可切换并渲染。
- AI 协作 Quick-Add：输入框与“交给 AI 团队”入口可用，真实创建编排并清理成功。
- 想法库“交给 AI 团队”转交入口存在；文件视图 tab 渲染正常；专家页列表与调用/编辑/复制/删除按钮齐全。
- 遗留：P1A 的“运行中交互回归 / 双客户端同会话提示 / 中断任务继续执行”建议用户真机抽查一次确认。

**队列二 S1 第一步（诊断增强，已部署）**：

- 子代理失败原因由“子代理结束原因：error”升级为结构化诊断
  （stopReason / error / message / errors 字段），任务卡与日志可直接看到失败细节，
  便于复现 worker2/worker3 的问题；下一步按诊断结果处理模型路由与子代理会话稳定性。

**队列二 S1 复现结论（2026-08-20）**：

- 用诊断版复现一次 2 子代理执行：两个子代理一次通过（gpt-5.4-mini），主代理 gpt-5.4 汇总成功，
  进入验收，无重试无错误；诊断编排已清理。
- 归因：此前失败与方案分配到 ai-gateway 的 gpt-5.3-codex-spark / gpt-5.2-pro 有关（子代理会话超时/报错），
  而非编排链路本身；worker 超时提升与改用 gpt-5.4 系列后稳定。
- 后续增强（加入队列三）：模型健康度跟踪——按 provider/model 统计执行失败，分配时降权近期失败模型。

**队列二 S2 评测集补题（2026-08-20，完成）**：

- 新增 8 条 published 知识条目：工作台-单多AI模式 / 对话流与浮层 / 项目配置 / 模型探测 /
  协作任务看板 / 规划健壮性 / 溯源门、知识库-项目经验。
- 评测集由 25 题补至 50 题（覆盖 P2.5/P2.6/P2.7 功能与项目经验），recall@5 = 1.0（50/50 命中）。

**队列三 F1 会话专属内容自动注入（2026-08-20，完成）**：

- `projectContextSummary` 增加会话维度：多 AI 规划提示词现在会注入当前会话的专属内容
  （`dsh-workbench-session.md`，截断 3000 字），与项目规则/备注并列；smoke 断言已补充。

### 执行建议（下一轮开始）

1. 先跑一轮完整 GUI 回归把 V1/V2 的实测结果拿到（我可代跑，你按清单抽查）。
2. 启动 S1 多代理稳定性专项（优先复现 worker2/worker3 的失败并加诊断日志）。
3. S2 评测集补题与 S3 使用反馈并行积累。
4. 功能与发布队列（F/M/R）在稳定性达标后按序推进，R5 朋友安装包与 P6 文档同步准备。
